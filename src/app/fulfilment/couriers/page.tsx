import {AppShell} from "@/components/app-shell";import {FulfilmentTabs} from "@/components/fulfilment-tabs";import {CourierForm} from "@/components/fulfilment-forms";import {getRows,SHEETS} from "@/lib/sheets";import {can,requirePermission} from "@/lib/authz";
export default async function Page(){const u=await requirePermission("fulfilment.view");const rows=(await getRows(SHEETS.fulfilmentCouriers)).filter((x:any)=>String(x.IsActive).toLowerCase()!=="false");return <AppShell active="Fulfilment"><main className="page"><div className="page-header"><div><h1 className="page-title">Couriers & Drivers</h1><p className="page-desc">Internal delivery resources and third-party courier partners.</p></div></div><FulfilmentTabs active="Couriers"/><div className="document-detail-grid"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Type</th><th>Contact</th><th>Vehicle / Ref.</th><th>Default fee</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.Id}><td>{r.Code}</td><td><strong>{r.Name}</strong></td><td>{r.CourierType}</td><td>{r.ContactName||"—"}<small style={{display:"block"}}>{r.Phone||""}</small></td><td>{r.VehicleReference||"—"}</td><td>₦{Number(r.DefaultFee||0).toLocaleString()}</td></tr>)}</tbody></table></div></section>{can(u,"fulfilment.couriers.manage")&&<aside className="card section"><h2>Add courier</h2><CourierForm/></aside>}</div></main></AppShell>}
