import {AppShell} from "@/components/app-shell";import {ReportsTabs} from "@/components/reports-tabs";import {requirePermission} from "@/lib/authz";import {customerReport} from "@/features/reports/data";
const money=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0}).format(Number(v||0));
export default async function Page(){await requirePermission("reports.customers.view");const x=await customerReport();const sales=x.rows.reduce((s,r)=>s+Number(r.Sales||0),0),outstanding=x.rows.reduce((s,r)=>s+Number(r.Outstanding||0),0);return <AppShell active="Reports & Analytics"><main className="page reports-page"><div className="page-header"><div><h1 className="page-title">Customer Analytics</h1><p className="page-desc">Customer value, transactions, outstanding balances, loyalty and engagement.</p></div><a className="btn btn-secondary" href="/api/reports/export?type=customers">Export CSV</a></div><ReportsTabs active="Customers"/><div className="grid-4"><div className="card kpi"><span className="kpi-label">Customers</span><strong className="kpi-value">{x.rows.length}</strong></div><div className="card kpi"><span className="kpi-label">Attributed sales</span><strong className="kpi-value">{money(sales)}</strong></div><div className="card kpi"><span className="kpi-label">Outstanding</span><strong className="kpi-value">{money(outstanding)}</strong></div><div className="card kpi"><span className="kpi-label">Loyalty points</span><strong className="kpi-value">{x.rows.reduce((s,r)=>s+r.LoyaltyPoints,0)}</strong></div></div><section className="card section" style={{marginTop:18}}><h2>Customer Value</h2><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Customer</th><th>Type</th><th>Phone</th><th>Transactions</th><th>Sales</th><th>Outstanding</th><th>Points</th></tr></thead><tbody>{x.rows.sort((a,b)=>b.Sales-a.Sales).map((r,i)=><tr key={i}><td>{r.Code}</td><td>{r.Customer}</td><td>{r.Type}</td><td>{r.Phone}</td><td>{r.Transactions}</td><td>{money(r.Sales)}</td><td>{money(r.Outstanding)}</td><td>{r.LoyaltyPoints}</td></tr>)}</tbody></table></div></section></main></AppShell>}
