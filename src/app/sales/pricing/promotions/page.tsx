import {AppShell} from "@/components/app-shell";import {SalesTabs} from "@/components/sales-tabs";import {PromotionForm} from "@/components/pricing-forms";import {requirePermission} from "@/lib/authz";import {pricingContext} from "@/features/pricing/data";
export default async function Page(){await requirePermission("pricing.view");const c=await pricingContext();return <AppShell active="Sales"><main className="page sales-page"><div className="page-header"><div><h1 className="page-title">Promotions</h1><p className="page-desc">Schedule product promotions with quantity thresholds and controlled discount values.</p></div></div><SalesTabs active="Pricing"/><div className="document-detail-grid"><section className="card section"><h3>Create promotion</h3><PromotionForm products={c.products}/></section><section className="card section"><h3>Governance</h3><div className="policy-list"><p>Promotion dates determine when a campaign is eligible.</p><p>Product-specific rules override broad promotional eligibility.</p><p>Discount rules are resolved server-side; UI prices are not authoritative.</p><p>Manual POS discounts remain subject to cashier/override permissions.</p></div></section></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Campaign</th><th>Period</th><th>Status</th><th>Rules</th></tr></thead><tbody>{c.promos.map((p:any)=><tr key={p.Id}><td><strong>{p.Code}</strong><span className="table-sub">{p.Name}</span></td><td>{p.StartAt||'Open'} → {p.EndAt||'Open'}</td><td>{p.Status}</td><td>{c.rules.filter((r:any)=>r.PromotionId===p.Id).length}</td></tr>)}</tbody></table></div></section></main></AppShell>}
