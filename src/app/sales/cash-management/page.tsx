import Link from "next/link";import { AppShell } from "@/components/app-shell";import { SalesTabs } from "@/components/sales-tabs";import { requirePermission } from "@/lib/authz";import { cashManagementOverview,money } from "@/features/sales/cash-management-data";import { EndOfDayForm } from "@/components/end-of-day-form";import { getRows,SHEETS } from "@/lib/sheets";
export default async function Page(){await requirePermission("sales.cash.view");const x=await cashManagementOverview();const branches=(await getRows(SHEETS.branches)).filter((b:any)=>String(b.IsActive??true)!=="false");return <AppShell active="Sales"><main className="page sales-page"><div className="page-header"><div><h1 className="page-title">Cash Management</h1><p className="page-desc">Control tills, cash movements, reconciliation, supervisor review and cashier settlement.</p></div></div><SalesTabs active="Cash Management"/><div className="grid-4"><div className="card kpi"><span>Open shifts</span><strong>{x.openShifts}</strong></div><div className="card kpi"><span>Variance reviews</span><strong>{x.pendingReviews}</strong></div><div className="card kpi"><span>Unsettled shifts</span><strong>{x.unsettled}</strong></div><div className="card kpi"><span>Today's till movements</span><strong>{x.todayMovements}</strong></div></div><section className="card section" style={{marginTop:16}}><div className="section-head"><div><h2>Shift reconciliation register</h2><p>Drill into a shift to review payment mix, till movements and settlement.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Shift</th><th>Cashier</th><th>Warehouse</th><th>Expected</th><th>Counted</th><th>Variance</th><th>Reconciliation</th><th></th></tr></thead><tbody>{x.shifts.map((r:any)=><tr key={r.Id}><td><strong>{r.ShiftNumber}</strong><span className="table-sub">{new Date(r.OpenedAt).toLocaleString()}</span></td><td>{r.CashierName}</td><td>{r.WarehouseName}</td><td>{money(r.ExpectedCash)}</td><td>{r.ClosingCash!==null&&r.ClosingCash!==""?money(r.ClosingCash):"—"}</td><td>{r.Variance!==null&&r.Variance!==""?money(r.Variance):"—"}</td><td><span className="badge badge-neutral">{r.ReconciliationStatus}</span></td><td><Link className="btn btn-secondary btn-sm" href={`/sales/cash-management/${r.Id}`}>Review</Link></td></tr>)}</tbody></table></div></section><section className="card section" style={{marginTop:16,maxWidth:560}}><h2>End-of-day close</h2><p>Close the business day only after all included cashier shifts are fully reconciled and closed.</p><EndOfDayForm branches={branches}/></section></main></AppShell>}
