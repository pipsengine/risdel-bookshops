import Link from "next/link";import { AppShell } from "@/components/app-shell";import { CatalogueTabs } from "@/components/catalogue-tabs";import { CatalogueImportForm } from "@/components/catalogue-import-form";import { requirePermission } from "@/lib/authz";import { catalogueImportHistory } from "@/features/catalogue/academic-data";
export default async function Page(){await requirePermission("catalogue.import.view");const jobs=await catalogueImportHistory();return <AppShell active="Catalogue"><main className="page catalogue-page"><div className="page-header"><div><h1 className="page-title">Catalogue import & export</h1><p className="page-desc">Controlled bulk onboarding and portable catalogue exports for migration, review and data stewardship.</p></div><Link className="btn btn-secondary" href="/api/catalogue/export">Export catalogue CSV</Link></div><CatalogueTabs active="Import / Export"/><div className="catalogue-layout"><section className="card section"><div className="section-heading"><div><h2>Import history</h2><p>Every attempted bulk import is logged for audit and troubleshooting.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Created</th><th>File</th><th>Mode</th><th>Total</th><th>Success</th><th>Errors</th><th>Status</th></tr></thead><tbody>{jobs.length?jobs.map((x:any)=><tr key={x.Id}><td>{String(x.CreatedAt||"").slice(0,16).replace("T"," ")}</td><td><strong>{x.FileName||"—"}</strong></td><td>{x.DryRun?"Validation":"Import"}</td><td>{x.TotalRows}</td><td>{x.SuccessRows}</td><td>{x.ErrorRows}</td><td><span className={`badge ${x.Status==="COMPLETED"?"badge-success":x.Status==="VALIDATED"?"badge-info":"badge-warning"}`}>{x.Status}</span></td></tr>):<tr><td colSpan={7}><div className="catalogue-empty"><strong>No import jobs yet.</strong><span>Validate your first catalogue CSV using the panel on the right.</span></div></td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Bulk product CSV</h2><p style={{marginBottom:16}}>Required columns: Code, Name, ProductType. Optional book fields can be included using the documented template.</p><CatalogueImportForm/><div className="import-template"><strong>Supported columns</strong><code>Code, Name, ProductType, CategoryCode, CostPrice, SellingPrice, ISBN13, ISBN10, PublisherCode, SubjectCode, AcademicLevelCode, ClassCode, CurriculumCode</code></div></aside></div></main></AppShell>}
