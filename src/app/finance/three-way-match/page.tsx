import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {payablesWorkspace} from "@/features/finance/payables-data";import {resolveMatch} from "@/app/actions/payables";export default async function Page(){await requirePermission("finance.three-way-match.view");const x=await payablesWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Three-Way Matching</h1><p className="page-desc">Compare purchase order, goods receipt and supplier invoice before payment approval.</p></div></div><FinanceTabs active="Three-Way Match"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Exception</th><th>Variance</th><th>Status</th><th>Resolution</th></tr></thead><tbody>{x.exceptions.map((e:any)=><tr key={e.Id}><td><strong>{e.ExceptionType}</strong><br/><small>{e.Description}</small></td><td>{e.VarianceAmount||0}</td><td>{e.Status}</td><td>{e.Status==="OPEN"?<form action={resolveMatch} style={{display:"flex",gap:8}}><input type="hidden" name="id" value={e.Id}/><input className="input" name="resolution" required placeholder="Resolution / approval reason"/><button className="btn btn-primary btn-sm">Resolve</button></form>:e.Resolution}</td></tr>)}</tbody></table></div></section></main></AppShell>}
