import { requirePermission } from "@/lib/authz";import { listProducts } from "@/features/catalogue/data";
function csv(v:any){const s=String(v??"");return /[",\n]/.test(s)?`"${s.replace(/"/g,'""')}"`:s}
export async function GET(){await requirePermission("catalogue.export");const rows=await listProducts();const headers=["Code","Name","ProductType","Category","Publisher","ISBN13","ISBN10","Barcode","CostPrice","SellingPrice","Status"];const lines=[headers.join(","),...rows.map((r:any)=>[r.Code,r.Name,r.ProductType,r.CategoryName,r.PublisherName,r.ISBN13,r.ISBN10,r.Barcode,r.CostPrice,r.SellingPrice,r.IsActive?"ACTIVE":"INACTIVE"].map(csv).join(","))];return new Response(lines.join("\n"),{headers:{"Content-Type":"text/csv; charset=utf-8","Content-Disposition":"attachment; filename=risdel-books-catalogue.csv"}})}
