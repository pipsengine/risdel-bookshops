import { AppShell } from "@/components/app-shell";import { AdminTabs } from "@/components/admin-tabs";import { requirePermission } from "@/lib/authz";import { loginHistory } from "@/features/auth/data";
function date(v:any){return new Intl.DateTimeFormat('en-NG',{dateStyle:'medium',timeStyle:'medium'}).format(new Date(v))}
export default async function Page(){await requirePermission("audit.view");const rows=await loginHistory();return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">Login history</h1><p className="page-desc">Review successful and failed sign-in attempts across Risdel Books.</p></div></div><AdminTabs active="Login History"/><section className="card section" style={{marginTop:18}}><div className="table-wrap"><table className="table"><thead><tr><th>Date & time</th><th>User</th><th>Email</th><th>Result</th><th>Reason</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={String(r.Id)}><td>{date(r.OccurredAt)}</td><td>{r.DisplayName||'Unknown'}</td><td>{r.Email}</td><td><span className={`badge ${r.WasSuccessful?'badge-success':'badge-danger'}`}>{r.WasSuccessful?'Successful':'Failed'}</span></td><td>{r.FailureReason||'—'}</td></tr>):<tr><td colSpan={5}><div className="empty-state"><strong>No login history yet</strong><span>Database-backed sign-in events will appear here.</span></div></td></tr>}</tbody></table></div></section></main></AppShell>}
