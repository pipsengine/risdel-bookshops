import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {closeWorkspace} from "@/features/finance/close-data";import {createAccrual,createPrepayment} from "@/app/actions/period-close";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.accruals.view");const x=await closeWorkspace();const expenses=x.coa.filter((a:any)=>a.AccountType==="EXPENSE"),assets=x.coa.filter((a:any)=>a.AccountType==="ASSET"),liabilities=x.coa.filter((a:any)=>a.AccountType==="LIABILITY");return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Accruals & Prepayments</h1><p className="page-desc">Period-end adjustment register for expenses incurred but unpaid and prepaid costs awaiting recognition.</p></div></div><FinanceTabs active="Accruals & Prepayments"/><div className="document-detail-grid"><section className="card section"><h2>Create accrual</h2><form action={createAccrual} className="form-grid"><label>Period<select name="periodId" required>{x.periods.map((p:any)=><option key={p.Id} value={p.Id}>{p.Name}</option>)}</select></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Expense account<select name="expenseAccountId" required>{expenses.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Liability account<select name="liabilityAccountId" required>{liabilities.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label className="full">Description<input name="description" required/></label><label>Reversal date<input name="reversalDate" type="date"/></label><button className="btn btn-primary">Create accrual</button></form></section><section className="card section"><h2>Create prepayment</h2><form action={createPrepayment} className="form-grid"><label>Amount<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Asset account<select name="assetAccountId" required>{assets.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Expense account<select name="expenseAccountId" required>{expenses.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Frequency<select name="frequency"><option>MONTHLY</option><option>QUARTERLY</option><option>ANNUAL</option></select></label><label>Start date<input name="startDate" type="date" required/></label><label>End date<input name="endDate" type="date" required/></label><label className="full">Description<input name="description" required/></label><button className="btn btn-primary">Create prepayment</button></form></section></div><section className="card section"><h2>Accrual register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Reference</th><th>Description</th><th>Amount</th><th>Status</th><th>Reversal</th></tr></thead><tbody>{x.accruals.length?x.accruals.map((a:any)=><tr key={a.Id}><td>{a.AccrualNumber}</td><td>{a.Description}</td><td>{m(a.Amount)}</td><td>{a.Status}</td><td>{a.ReversalDate||"—"}</td></tr>):<tr><td colSpan={5}>No accruals recorded.</td></tr>}</tbody></table></div></section><section className="card section"><h2>Prepayment register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Reference</th><th>Description</th><th>Original</th><th>Remaining</th><th>Period</th><th>Status</th></tr></thead><tbody>{x.prepayments.length?x.prepayments.map((p:any)=><tr key={p.Id}><td>{p.PrepaymentNumber}</td><td>{p.Description}</td><td>{m(p.OriginalAmount)}</td><td>{m(p.RemainingAmount)}</td><td>{p.StartDate} → {p.EndDate}</td><td>{p.Status}</td></tr>):<tr><td colSpan={6}>No prepayments recorded.</td></tr>}</tbody></table></div></section></main></AppShell>}
