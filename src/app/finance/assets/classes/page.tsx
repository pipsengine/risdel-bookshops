import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {assetWorkspace} from "@/features/finance/assets-data";import {createAssetClass} from "@/app/actions/fixed-assets";export default async function Page(){await requirePermission("finance.assets.configure");const x=await assetWorkspace(),assets=x.coa.filter((a:any)=>a.AccountType==='ASSET'),expenses=x.coa.filter((a:any)=>a.AccountType==='EXPENSE');return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Fixed Asset Classes</h1><p className="page-desc">Control useful lives, methods and GL mappings for asset capitalization and depreciation.</p></div></div><FinanceTabs active="Fixed Assets"/><section className="card section"><form action={createAssetClass} className="form-grid"><label>Code<input name="code" required/></label><label>Name<input name="name" required/></label><label>Useful life (months)<input type="number" min="1" name="usefulLifeMonths" required/></label><label>Method<select name="depreciationMethod"><option value="STRAIGHT_LINE">Straight line</option><option value="REDUCING_BALANCE">Reducing balance</option></select></label><label>Residual value %<input type="number" min="0" max="100" step="0.01" name="residualValuePercent"/></label><label>Annual rate % (reducing balance)<input type="number" min="0" step="0.01" name="annualRate"/></label><label>Asset account<select name="assetAccountId" required>{assets.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Accumulated depreciation<select name="accumDepAccountId" required>{assets.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Depreciation expense<select name="depExpenseAccountId" required>{expenses.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Impairment expense<select name="impairmentExpenseAccountId" required>{expenses.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label className="full">Description<input name="description"/></label><button className="btn btn-primary">Create class</button></form></section><section className="card section"><h2>Configured classes</h2><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Life</th><th>Method</th><th>Residual</th></tr></thead><tbody>{x.classes.map((c:any)=><tr key={c.Id}><td>{c.Code}</td><td>{c.Name}</td><td>{c.UsefulLifeMonths} months</td><td>{c.DepreciationMethod}</td><td>{c.ResidualValuePercent||0}%</td></tr>)}</tbody></table></div></section></main></AppShell>}
