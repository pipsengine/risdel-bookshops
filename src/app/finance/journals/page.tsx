import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {glWorkspace} from "@/features/finance/gl-data";import {createManualJournal} from "@/app/actions/general-ledger";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.journals.view");const x=await glWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Journals</h1><p className="page-desc">Balanced manual and automated accounting journal register.</p></div></div><FinanceTabs active="Journals"/><section className="card section"><div className="section-head"><div><h2>Manual journal</h2><p>Post controlled accounting adjustments using two balanced lines.</p></div></div><form action={createManualJournal} className="form-grid"><label>Date<input name="date" type="date" required/></label><label>Debit account<select name="debitAccount" required><option value="">Select</option>{x.coa.map((a:any)=><option key={a.Id} value={a.AccountCode}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Credit account<select name="creditAccount" required><option value="">Select</option>{x.coa.map((a:any)=><option key={a.Id} value={a.AccountCode}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" required/></label><label className="span-2">Description<input name="description" required/></label><div><button className="btn btn-primary">Post journal</button></div></form></section><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Journal</th><th>Date</th><th>Type</th><th>Description</th><th>Debit</th><th>Credit</th><th>Status</th></tr></thead><tbody>{x.journals.length?x.journals.map((j:any)=><tr key={j.Id}><td>{j.JournalNumber}</td><td>{j.JournalDate}</td><td>{j.JournalType}</td><td>{j.Description}</td><td>{m(j.TotalDebit)}</td><td>{m(j.TotalCredit)}</td><td>{j.Status}</td></tr>):<tr><td colSpan={7}>No journals posted yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
