import { AppShell } from "@/components/app-shell";import { appConfig } from "@/config/app";import { databaseHealth,databaseConfigured } from "@/lib/db";
export default async function SystemInfo(){const db=await databaseHealth();return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">System information</h1><p className="page-desc">Environment, platform configuration and foundation health.</p></div><span className="badge badge-success">Operational</span></div><div className="system-grid"><section className="card section"><h2>Application</h2><p>Current application identity and runtime.</p><div style={{marginTop:14}}><div className="setting-row"><span>System</span><strong>{appConfig.name}</strong></div><div className="setting-row"><span>Company</span><strong>{appConfig.company}</strong></div><div className="setting-row"><span>Version</span><strong>{appConfig.version}</strong></div><div className="setting-row"><span>Environment</span><strong style={{textTransform:'capitalize'}}>{appConfig.environment}</strong></div><div className="setting-row"><span>Timezone</span><strong>{appConfig.timezone}</strong></div><div className="setting-row"><span>Currency</span><strong>{appConfig.currency}</strong></div></div></section><section className="card section"><h2>Service health</h2><p>Connection readiness without exposing credentials.</p><div style={{marginTop:14}}><div className="setting-row"><span>Application</span><strong><span className="badge badge-success">Healthy</span></strong></div><div className="setting-row"><span>SQL configured</span><strong>{databaseConfigured()?"Yes":"No"}</strong></div><div className="setting-row"><span>SQL status</span><strong><span className={`badge ${db.status==='Healthy'?'badge-success':'badge-warning'}`}>{db.status}</span></strong></div><div className="setting-row"><span>Storage</span><strong><span className="badge badge-success">Ready</span></strong></div></div></section></div><section className="card section" style={{marginTop:18}}><h2>Database connection detail</h2><p>{db.detail}</p></section></main></AppShell>}
