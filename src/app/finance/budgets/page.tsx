import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {budgetsWorkspace} from "@/features/finance/management-data";import {addBudgetLine,approveBudget,createBudget} from "@/app/actions/management-accounts";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.budgets.view");const x=await budgetsWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Budgets</h1><p className="page-desc">Account and period based financial budgets with approval control.</p></div></div><FinanceTabs active="Budgets"/><div className="document-detail-grid"><section className="card section"><h2>Create budget</h2><form action={createBudget} className="form-grid"><label>Name<input name="name" required placeholder="FY 2027 Operating Budget"/></label><label>Fiscal year<input name="year" defaultValue={new Date().getFullYear()+1}/></label><label className="full">Description<textarea name="description"/></label><button className="btn btn-primary" type="submit">Create budget</button></form></section><section className="card section"><h2>Add / update budget line</h2><form action={addBudgetLine} className="form-grid"><label>Budget<select name="budgetId" required>{x.budgets.map((b:any)=><option key={b.Id} value={b.Id}>{b.BudgetCode} — {b.Name}</option>)}</select></label><label>Account<select name="accountId" required>{x.coa.map((a:any)=><option key={a.Id} value={a.Id}>{a.AccountCode} — {a.AccountName}</option>)}</select></label><label>Period<select name="periodCode" required>{x.periods.map((p:any)=><option key={p.Id} value={p.PeriodCode}>{p.Name}</option>)}</select></label><label>Amount<input name="amount" type="number" min="0" step="0.01" required/></label><label className="full">Notes<input name="notes"/></label><button className="btn btn-primary" type="submit">Save line</button></form></section></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Budget</th><th>Year</th><th>Status</th><th>Version</th><th>Lines</th><th>Total</th><th>Action</th></tr></thead><tbody>{x.budgets.length?x.budgets.map((b:any)=><tr key={b.Id}><td><strong>{b.BudgetCode}</strong><br/>{b.Name}</td><td>{b.FiscalYear}</td><td><span className={`badge ${b.Status==='APPROVED'?'badge-success':'badge-warning'}`}>{b.Status}</span></td><td>{b.Version}</td><td>{b.Lines.length}</td><td>{m(b.Total)}</td><td>{b.Status!=='APPROVED'?<form action={approveBudget}><input type="hidden" name="id" value={b.Id}/><button className="btn btn-sm" type="submit">Approve</button></form>:"Approved"}</td></tr>):<tr><td colSpan={7}>No budgets created yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
