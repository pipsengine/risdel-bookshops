import Link from "next/link";import { AppShell } from "@/components/app-shell";import { CustomerTabs } from "@/components/customer-tabs";import { requirePermission } from "@/lib/authz";import { loyaltySummary } from "@/features/customers/data";
export default async function Page(){await requirePermission("crm.loyalty.view");const data=await loyaltySummary();return <AppShell active="Customers"><main className="page crm-page"><div className="page-header"><div><h1 className="page-title">Loyalty</h1><p className="page-desc">Track customer points and loyalty tiers. Earning rules can be automated in a later promotions module.</p></div></div><CustomerTabs active="Loyalty"/><div className="crm-two-col"><section className="card section"><h2>Members</h2><div className="table-wrap"><table className="table"><thead><tr><th>Customer</th><th>Tier</th><th>Points</th></tr></thead><tbody>{data.customers.map(c=><tr key={c.Id}><td><Link href={`/customers/${c.Id}`} className="back-link">{c.Name}</Link><span className="table-sub">{c.CustomerCode}</span></td><td>{c.LoyaltyTier||"—"}</td><td><strong>{c.LoyaltyPoints}</strong></td></tr>)}{!data.customers.length&&<tr><td colSpan={3}>No loyalty members yet.</td></tr>}</tbody></table></div></section><section className="card section"><h2>Recent movements</h2>{data.recent.map(r=><div className="crm-mini-row" key={r.Id}><div><strong>{r.TransactionType}</strong><small>{String(r.CreatedAt).slice(0,10)} · {r.Reason||r.Reference}</small></div><b>{Number(r.Points)>0?"+":""}{r.Points}</b></div>)}{!data.recent.length&&<p className="section-copy">No loyalty movements yet.</p>}</section></div></main></AppShell>}
