import {AppShell} from "@/components/app-shell";import {InventoryTabs} from "@/components/inventory-tabs";import {requirePermission} from "@/lib/authz";import {inventoryMoney,stockOverview} from "@/features/inventory/data";
export default async function Page(){await requirePermission('inventory.reorder.view');const rows=(await stockOverview({status:'LOW'})).sort((a:any,b:any)=>b.RecommendedOrder-a.RecommendedOrder);return <AppShell active="Inventory"><main className="page"><div className="page-header"><div><h1 className="page-title">Reorder Planning</h1><p className="page-desc">Prioritise replenishment using available stock, reorder levels and safety stock.</p></div><span className="badge badge-warning">{rows.length} action items</span></div><InventoryTabs active="Reorder Planning"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Product</th><th>Warehouse</th><th>Available</th><th>Reorder level</th><th>Safety stock</th><th>Recommended order</th><th>Est. replenishment cost</th><th>Priority</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.Id}><td><strong>{r.ProductName}</strong><span className="table-sub">{r.ProductCode}</span></td><td>{r.WarehouseName}</td><td>{r.Available}</td><td>{r.ReorderLevel}</td><td>{r.SafetyStock}</td><td><strong>{r.RecommendedOrder}</strong></td><td>{inventoryMoney(r.RecommendedOrder*r.AverageCost)}</td><td><span className={`badge ${r.OutOfStock?'badge-danger':'badge-warning'}`}>{r.OutOfStock?'Critical':'Reorder'}</span></td></tr>):<tr><td colSpan={8}><div className="catalogue-empty"><strong>No replenishment action required.</strong><span>All stock positions are currently above their reorder thresholds.</span></div></td></tr>}</tbody></table></div></section></main></AppShell>}
