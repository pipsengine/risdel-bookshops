import {AppShell} from "@/components/app-shell";import {CustomerTabs} from "@/components/customer-tabs";import {LoyaltyTabs} from "@/components/loyalty-tabs";import {CampaignForms} from "@/components/loyalty-forms";import {requirePermission} from "@/lib/authz";import {loyaltyAdmin} from "@/features/customers/loyalty-data";
export default async function Page(){await requirePermission("crm.loyalty.view");const d=await loyaltyAdmin();return <AppShell active="Customers"><main className="page"><div className="page-header"><div><h1 className="page-title">Reward Campaigns</h1><p className="page-desc">Run birthday, seasonal and customer-segment reward programmes with duplicate-award protection.</p></div></div><CustomerTabs active="Loyalty"/><LoyaltyTabs active="Campaigns"/><CampaignForms segments={d.segments} campaigns={d.campaigns}/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Campaign</th><th>Type</th><th>Bonus</th><th>Period</th><th>Status</th></tr></thead><tbody>{d.campaigns.map(c=><tr key={c.Id}><td><strong>{c.Name}</strong><span className="table-sub">{c.Code}</span></td><td>{c.CampaignType}</td><td>{Number(c.BonusPoints||0).toLocaleString()} pts</td><td>{c.StartAt||"—"} → {c.EndAt||"—"}</td><td>{c.Status}</td></tr>)}{!d.campaigns.length&&<tr><td colSpan={5}>No reward campaigns configured.</td></tr>}</tbody></table></div></section></main></AppShell>}
