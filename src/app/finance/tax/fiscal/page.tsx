import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {TaxTabs} from "@/components/tax-tabs";import {requirePermission} from "@/lib/authz";import {taxWorkspace} from "@/features/finance/tax-data";import {updateFiscalSetting} from "@/app/actions/tax";
export default async function Page(){await requirePermission("tax.fiscal.manage");const x=await taxWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Fiscal Controls</h1><p className="page-desc">Effective-dated fiscalization and tax-engine settings with provider-ready controls.</p></div></div><FinanceTabs active="Tax & Compliance"/><TaxTabs active="Fiscal Controls"/><div className="document-detail-grid"><section className="card section"><h2>Fiscal / tax settings</h2><div className="table-wrap"><table className="table"><thead><tr><th>Setting</th><th>Value</th><th>Description</th><th>Update</th></tr></thead><tbody>{x.fiscalSettings.map((r:any)=><tr key={r.Id}><td><strong>{r.SettingKey}</strong></td><td>{String(r.SettingValue)}</td><td>{r.Description}</td><td><form action={updateFiscalSetting} style={{display:'flex',gap:6}}><input type="hidden" name="id" value={r.Id}/><input name="value" defaultValue={String(r.SettingValue)} style={{maxWidth:120}}/><button className="btn btn-sm">Save</button></form></td></tr>)}</tbody></table></div></section><aside className="card section"><h2>Fiscalization readiness</h2><div className="policy-list"><p>Fiscal documents are tracked separately from commercial invoices/receipts.</p><p>Provider integration is intentionally disabled until Risdel selects and configures an approved fiscalization path.</p><p>Every fiscal record can retain the source document, taxable amount, VAT, zero-rated amount and provider reference.</p><p>Do not enable fiscalization simply by changing a flag unless the integration endpoint and compliance process have been validated.</p></div></aside></div><section className="card section" style={{marginTop:16}}><h2>Fiscal document register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Source</th><th>Fiscal number</th><th>Status</th><th>Taxable</th><th>Tax</th><th>Zero-rated</th><th>Fiscalized</th></tr></thead><tbody>{x.fiscalDocs.length?x.fiscalDocs.map((r:any)=><tr key={r.Id}><td>{r.SourceNumber}</td><td>{r.FiscalNumber||'—'}</td><td>{r.FiscalStatus}</td><td>{r.TaxableAmount}</td><td>{r.TaxAmount}</td><td>{r.ZeroRatedAmount}</td><td>{r.FiscalizedAt||'—'}</td></tr>):<tr><td colSpan={7}>No fiscal documents generated yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
