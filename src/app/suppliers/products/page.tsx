import Link from "next/link";import {AppShell} from "@/components/app-shell";import {SupplierTabs} from "@/components/supplier-tabs";import {requirePermission} from "@/lib/authz";import {supplierMoney,supplierProductMatrix} from "@/features/suppliers/data";
export default async function Page(){await requirePermission("suppliers.view");const rows=await supplierProductMatrix();return <AppShell active="Suppliers"><main className="page"><div className="page-header"><div><h1 className="page-title">Supplier Product Matrix</h1><p className="page-desc">See which suppliers can source each product, their quoted cost, minimum order quantity and expected lead time.</p></div></div><SupplierTabs active="Product Matrix"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Product</th><th>Supplier</th><th>Supplier SKU</th><th>Unit cost</th><th>MOQ</th><th>Lead time</th><th>Source</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.Id}><td>{r.ProductName}</td><td><Link className="back-link" href={`/suppliers/${r.SupplierId}`}>{r.SupplierName}</Link></td><td>{r.SupplierSKU||"—"}</td><td>{supplierMoney(r.UnitCost)}</td><td>{r.MinOrderQty||1}</td><td>{r.LeadTimeDays||0} days</td><td><span className={`badge ${String(r.IsPreferred).toLowerCase()==="true"?"badge-success":""}`}>{String(r.IsPreferred).toLowerCase()==="true"?"Preferred":"Alternative"}</span></td></tr>):<tr><td colSpan={7}>No supplier-product relationships have been configured.</td></tr>}</tbody></table></div></section></main></AppShell>}
