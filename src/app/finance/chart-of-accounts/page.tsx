import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financeOverview} from "@/features/finance/data";export default async function Page(){await requirePermission("finance.view");const x=await financeOverview();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Chart of Accounts</h1><p className="page-desc">Controlled account structure for future automated and manual postings.</p></div></div><FinanceTabs active="Chart of Accounts"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Account</th><th>Type</th><th>Normal balance</th><th>Control</th></tr></thead><tbody>{x.coa.map((a:any)=><tr key={a.Id}><td><strong>{a.AccountCode}</strong></td><td>{a.AccountName}</td><td>{a.AccountType}</td><td>{a.NormalBalance}</td><td>{String(a.IsControlAccount)==='true'?'Yes':'No'}</td></tr>)}</tbody></table></div></section></main></AppShell>}
