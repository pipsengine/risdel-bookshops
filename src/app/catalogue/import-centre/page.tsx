import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { CatalogueTabs } from "@/components/catalogue-tabs";
import { AdvancedImportForm,RollbackImportForm } from "@/components/advanced-import-form";
import { requirePermission } from "@/lib/authz";
import { advancedImportDashboard } from "@/features/catalogue/import-data";
export default async function Page(){await requirePermission("catalogue.bulk-import.view");const d=await advancedImportDashboard();return <AppShell active="Catalogue"><main className="page catalogue-page"><div className="page-header"><div><h1 className="page-title">Inventory onboarding centre</h1><p className="page-desc">Validate and post Risdel Excel/CSV catalogue, category taxonomy and warehouse opening stock in one controlled batch.</p></div><div className="page-actions"><Link className="btn btn-secondary" href="/templates/Risdel_Bookshop_Nigeria_Inventory_Master.xlsx">Download Nigeria inventory template</Link></div></div><CatalogueTabs active="Inventory Import"/><div className="kpi-grid four"><div className="kpi-card"><span>Batches</span><strong>{d.summary.batches}</strong></div><div className="kpi-card"><span>Products imported</span><strong>{d.summary.products}</strong></div><div className="kpi-card"><span>Opening-stock lines</span><strong>{d.summary.stockLines}</strong></div><div className="kpi-card"><span>Open validation issues</span><strong>{d.summary.errors}</strong></div></div><div className="catalogue-layout"><section className="card section"><div className="section-heading"><div><h2>Import batches</h2><p>Dry-run validation, posting results and controlled rollback history.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Batch</th><th>File</th><th>Mode</th><th>Products</th><th>Stock</th><th>Errors</th><th>Status</th><th>Action</th></tr></thead><tbody>{d.batches.length?d.batches.map((x:any)=><tr key={x.Id}><td><strong>{x.BatchNumber}</strong><small className="table-sub">{String(x.StartedAt||"").slice(0,16).replace("T"," ")}</small></td><td>{x.FileName}</td><td>{x.DryRun?"Validation":x.ImportMode}</td><td>{x.CreatedProducts||0}</td><td>{x.PostedStockLines||0}</td><td>{x.ErrorRows||0}</td><td><span className={`badge ${String(x.Status).includes("COMPLETED")?"badge-success":String(x.Status).includes("ERROR")?"badge-warning":"badge-info"}`}>{x.Status}</span></td><td>{!x.DryRun&&String(x.Status).startsWith("COMPLETED")?<RollbackImportForm batchId={x.Id}/>:"—"}</td></tr>):<tr><td colSpan={8}>No advanced import batches yet.</td></tr>}</tbody></table></div><div className="section-heading" style={{marginTop:24}}><div><h2>Validation issues</h2><p>Warnings and errors captured with the source workbook row.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Batch</th><th>Sheet / row</th><th>Key</th><th>Severity</th><th>Issue</th></tr></thead><tbody>{d.errors.length?d.errors.map((e:any)=><tr key={e.Id}><td>{e.BatchId?.slice(0,8)}</td><td>{e.SheetName} / {e.RowNumber}</td><td>{e.EntityKey||"—"}</td><td><span className={`badge ${e.Severity==="ERROR"?"badge-danger":"badge-warning"}`}>{e.Severity}</span></td><td>{e.Message}</td></tr>):<tr><td colSpan={5}>No unresolved import issues.</td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Upload inventory workbook</h2><p style={{marginBottom:16}}>Start with validation-only. When clean, untick validation and optionally post category hierarchy and opening stock.</p><AdvancedImportForm/><div className="import-template"><strong>Workbook sheets</strong><code>01_Catalogue_Upload<br/>02_Opening_Stock<br/>03_Category_Taxonomy<br/>06_Risdel_Reference_Codes</code></div></aside></div></main></AppShell>}
