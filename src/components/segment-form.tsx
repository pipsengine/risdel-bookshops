"use client";import { useActionState } from "react";import { createSegment,type CustomerActionState } from "@/app/actions/customers";const initial:CustomerActionState={};export function SegmentForm(){const [state,action,pending]=useActionState(createSegment,initial);return <form action={action} className="stack-form">{state.error&&<div className="alert alert-danger">{state.error}</div>}{state.success&&<div className="alert alert-success">{state.success}</div>}<label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Code</span><input name="code" placeholder="VIP_CUSTOMERS"/></label><label className="field"><span>Segment type</span><select name="segmentType"><option>MANUAL</option><option>BEHAVIOURAL</option><option>INSTITUTIONAL</option><option>CAMPAIGN</option></select></label><label className="field"><span>Description</span><textarea name="description" rows={4}/></label><button className="btn btn-primary full" disabled={pending}>Create segment</button></form>}
