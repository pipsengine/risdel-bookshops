import {AppShell} from "@/components/app-shell";import {SalesTabs} from "@/components/sales-tabs";import {ReservationForm} from "@/components/reservation-forms";import {listReservations,reservationLookups,reservationSummary,resMoney} from "@/features/sales/reservations-data";import {requirePermission} from "@/lib/authz";import {updateReservationStatus,processExpiredReservations} from "@/app/actions/reservations";
export default async function Page(){await requirePermission("sales.reservations.view");const [rows,l,s]=await Promise.all([listReservations(),reservationLookups(),reservationSummary()]);return <AppShell active="Sales"><main className="page"><div className="page-heading"><div><h1>Reservations</h1><p>Hold available stock for customers with controlled expiry, deposits and release.</p></div></div><SalesTabs active="Reservations"/><div className="page-actions"><form action={processExpiredReservations}><button className="btn">Process expired reservations</button></form></div><div className="kpi-grid"><div className="kpi-card"><span>Active</span><strong>{s.active}</strong></div><div className="kpi-card"><span>Expired attention</span><strong>{s.expired}</strong></div><div className="kpi-card"><span>Ready</span><strong>{s.ready}</strong></div><div className="kpi-card"><span>Deposits received</span><strong>{resMoney(s.deposits)}</strong></div></div><div className="content-grid two-thirds"><section className="panel"><div className="panel-head"><div><h2>Reservation register</h2><p>Current and recently managed reservations.</p></div></div><div className="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Product</th><th>Warehouse</th><th>Qty</th><th>Expiry</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(r=><tr key={r.Id}><td><strong>{r.ReservationNumber}</strong></td><td>{r.CustomerName}</td><td>{r.ProductName}</td><td>{r.WarehouseName}</td><td>{r.Quantity}</td><td>{r.ExpiresAt?new Date(r.ExpiresAt).toLocaleString("en-NG"):"—"}</td><td><span className={`badge ${r.Expired?"danger":""}`}>{r.Expired?"EXPIRED":r.Status}</span></td><td><form action={updateReservationStatus} className="inline-form"><input type="hidden" name="id" value={r.Id}/><select name="status" defaultValue={r.Status}><option>ACTIVE</option><option>READY</option><option>COLLECTED</option><option>RELEASED</option><option>EXPIRED</option><option>CANCELLED</option></select><button className="btn btn-small">Update</button></form></td></tr>)}</tbody></table>{!rows.length&&<div className="empty-state">No reservations yet.</div>}</div></section><aside className="panel"><h2>New reservation</h2><p>Reserve only stock that is physically available.</p><ReservationForm customers={l.customers} products={l.products} warehouses={l.warehouses}/></aside></div></main></AppShell>}
