import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {PettyCashFundForm,ReplenishmentForm} from "@/components/expense-forms";import {approveAndPostReplenishment} from "@/app/actions/expenses";import {expenseWorkspace,money} from "@/features/finance/expenses-data";import {requirePermission} from "@/lib/authz";
export default async function Page(){await requirePermission("finance.petty-cash.view");const x=await expenseWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Petty Cash</h1><p className="page-desc">Controlled imprest funds, custodians, balances, transactions and replenishment.</p></div></div><FinanceTabs active="Petty Cash"/><div className="grid-4">{x.funds.slice(0,4).map((f:any)=><div className="card kpi" key={f.Id}><span>{f.Name}</span><strong>{money(f.CurrentBalance)}</strong><small>{f.BranchName} · limit {money(f.FloatLimit)}</small></div>)}</div><div className="document-detail-grid" style={{marginTop:16}}><section><div className="card section"><h2>Petty cash funds</h2><div className="table-wrap"><table className="table"><thead><tr><th>Fund</th><th>Branch</th><th>Custodian</th><th>Limit</th><th>Balance</th><th>Reorder level</th><th>Status</th></tr></thead><tbody>{x.funds.map((f:any)=><tr key={f.Id}><td><strong>{f.FundCode}</strong><small style={{display:"block"}}>{f.Name}</small></td><td>{f.BranchName}</td><td>{f.CustodianName}</td><td>{money(f.FloatLimit)}</td><td>{money(f.CurrentBalance)}</td><td>{money(f.ReorderLevel)}</td><td><span className="badge badge-neutral">{f.Status}</span></td></tr>)}</tbody></table></div></div><div className="card section" style={{marginTop:16}}><h2>Pending replenishments</h2><div className="table-wrap"><table className="table"><thead><tr><th>Reference</th><th>Fund</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>{x.replenishments.filter((r:any)=>r.Status==="PENDING_APPROVAL").map((r:any)=><tr key={r.Id}><td>{r.ReplenishmentNumber}</td><td>{x.funds.find((f:any)=>f.Id===r.FundId)?.Name||"—"}</td><td>{money(r.RequestedAmount)}</td><td>{r.Status}</td><td><form action={approveAndPostReplenishment}><input type="hidden" name="id" value={r.Id}/><input name="reference" placeholder="Funding ref"/><button className="btn btn-sm">Approve & post</button></form></td></tr>)}</tbody></table></div></div></section><aside className="procurement-form-stack"><div className="card section"><h2>Create fund</h2><PettyCashFundForm branches={x.branches} users={x.users} cash={x.cash}/></div><div className="card section"><h2>Request replenishment</h2><ReplenishmentForm funds={x.funds}/></div></aside></div></main></AppShell>}
