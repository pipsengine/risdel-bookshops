import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {payablesWorkspace} from "@/features/finance/payables-data";import {approveSupplierInvoice} from "@/app/actions/payables";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.supplier-invoices.view");const x=await payablesWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Supplier Invoices</h1><p className="page-desc">Invoice register, matching status, approval and payable creation.</p></div></div><FinanceTabs active="Supplier Invoices"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Invoice</th><th>Supplier</th><th>Total</th><th>Match</th><th>Status</th><th>Action</th></tr></thead><tbody>{x.invoices.map((r:any)=><tr key={r.Id}><td><strong>{r.InvoiceNumber}</strong><br/><small>{r.InternalNumber}</small></td><td>{r.SupplierName}</td><td>{m(r.Total)}</td><td><span className="badge badge-neutral">{r.MatchStatus}</span></td><td>{r.Status}</td><td>{r.Status==="PENDING_APPROVAL"?<form action={approveSupplierInvoice}><input type="hidden" name="id" value={r.Id}/><button className="btn btn-primary btn-sm">Approve</button></form>:"—"}</td></tr>)}</tbody></table></div></section></main></AppShell>}
