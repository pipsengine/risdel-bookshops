import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financeOverview} from "@/features/finance/data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.receivables.view");const x=await financeOverview();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Accounts Receivable</h1><p className="page-desc">Customer invoice balances and collection exposure.</p></div></div><FinanceTabs active="Receivables"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Invoice</th><th>Customer</th><th>Total</th><th>Paid</th><th>Balance</th><th>Status</th></tr></thead><tbody>{x.receivables.map((r:any)=><tr key={r.Id}><td><strong>{r.InvoiceNumber}</strong></td><td>{x.customers.find((c:any)=>c.Id===r.CustomerId)?.Name||"Customer"}</td><td>{m(r.Total)}</td><td>{m(r.PaidAmount)}</td><td>{m(r.Balance)}</td><td><span className="badge badge-neutral">{r.Status}</span></td></tr>)}</tbody></table></div></section></main></AppShell>}
