import {AppShell} from "@/components/app-shell";import {AcademicSalesTabs} from "@/components/academic-sales-tabs";import {requirePermission} from "@/lib/authz";import {availability} from "@/features/academic-sales/data";
export default async function Page(){await requirePermission("academic.sales.view");const rows=await availability();const out=rows.filter(x=>x.Status==="OUT OF STOCK").length,partial=rows.filter(x=>x.Status==="PARTIAL").length,ok=rows.filter(x=>x.Status==="AVAILABLE").length;return <AppShell active="Academic Sales"><main className="page academic-sales-page"><div className="page-header"><div><h1 className="page-title">Book-List Availability</h1><p className="page-desc">Compare active school requirements with current available-to-sell inventory before committing orders.</p></div></div><AcademicSalesTabs active="Availability"/><div className="sales-kpis academic-kpis"><div className="card mini-kpi"><span>Available</span><strong>{ok}</strong><small>Requirements fully coverable</small></div><div className="card mini-kpi"><span>Partial</span><strong>{partial}</strong><small>Insufficient available quantity</small></div><div className="card mini-kpi"><span>Out of stock</span><strong>{out}</strong><small>Procurement attention required</small></div><div className="card mini-kpi"><span>Total requirements</span><strong>{rows.length}</strong><small>Book-list product lines</small></div></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Book / Product</th><th>ISBN</th><th>School</th><th>Book list</th><th>Required</th><th>Available</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.Id}><td><strong>{r.ProductName}</strong></td><td>{r.ISBN}</td><td>{r.SchoolName}</td><td>{r.ListName}</td><td>{r.Required}</td><td>{r.Available}</td><td><span className={`badge ${r.Status==="AVAILABLE"?"badge-success":r.Status==="PARTIAL"?"badge-warning":"badge-danger"}`}>{r.Status}</span></td></tr>):<tr><td colSpan={7}><div className="catalogue-empty"><strong>No book-list requirements yet.</strong><span>Add products to a school book list to begin availability planning.</span></div></td></tr>}</tbody></table></div></section></main></AppShell>}
