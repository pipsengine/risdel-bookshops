import {AppShell} from "@/components/app-shell";import {AcademicSalesTabs} from "@/components/academic-sales-tabs";import {AcademicOrderForm} from "@/components/academic-sales-forms";import {requirePermission} from "@/lib/authz";import {listBookLists,listOrders,listSchools,money} from "@/features/academic-sales/data";import {updateAcademicOrder} from "@/app/actions/academic-sales";
export default async function Page(){await requirePermission("academic.sales.view");const [schools,lists,orders]=await Promise.all([listSchools(),listBookLists(),listOrders()]);return <AppShell active="Academic Sales"><main className="page academic-sales-page"><div className="page-header"><div><h1 className="page-title">Academic Orders</h1><p className="page-desc">Generate complete list orders for schools, parents or students and manage fulfilment status.</p></div><span className="badge badge-info">{orders.length} orders</span></div><AcademicSalesTabs active="Orders"/><div className="academic-sales-grid"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>School</th><th>Customer / Student</th><th>Lines</th><th>Total</th><th>Balance</th><th>Status</th><th>Action</th></tr></thead><tbody>{orders.length?orders.map(o=><tr key={o.Id}><td><strong>{o.OrderNumber}</strong><span className="table-sub">{String(o.OrderType).replaceAll("_"," ")}</span></td><td>{o.SchoolName}</td><td>{o.StudentName||`${o.StudentCount||1} list(s)`}</td><td>{o.LineCount}</td><td>{money(o.Total)}</td><td>{money(o.BalanceDue)}</td><td><span className="badge">{o.Status}</span></td><td><form action={updateAcademicOrder} className="academic-inline-action"><input type="hidden" name="id" value={o.Id}/><select name="status" defaultValue={o.Status}><option>PENDING</option><option>CONFIRMED</option><option>PROCESSING</option><option>READY</option><option>COMPLETED</option><option>CANCELLED</option></select><button className="btn btn-ghost">Save</button></form></td></tr>):<tr><td colSpan={8}><div className="catalogue-empty"><strong>No academic orders yet.</strong><span>Generate an order from an approved book list.</span></div></td></tr>}</tbody></table></div></section><aside className="card section"><h2>Generate order</h2><p className="section-copy">Student count multiplies every required quantity on the selected list, making the same workflow suitable for one parent or a whole school.</p><AcademicOrderForm schools={schools} lists={lists}/></aside></div></main></AppShell>}
