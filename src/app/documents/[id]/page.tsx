import {notFound} from "next/navigation";import {AppShell} from "@/components/app-shell";import {requirePermission} from "@/lib/authz";import {documentById} from "@/features/documents/data";import {archiveDocument,addDocumentLink} from "@/app/actions/documents";export default async function Page({params}:{params:Promise<{id:string}>}){await requirePermission("documents.view");const {id}=await params,x=await documentById(id);if(!x)notFound();const d:any=x.document;return <AppShell active="Documents"><main className="page"><div className="page-header"><div><h1 className="page-title">{d.DocumentNumber}</h1><p className="page-desc">{d.Title}</p></div><form action={archiveDocument}><input type="hidden" name="id" value={id}/><input type="hidden" name="restore" value={d.Status==='ARCHIVED'?'1':'0'}/><button className="btn btn-secondary">{d.Status==='ARCHIVED'?'Restore':'Archive'}</button></form></div><div className="grid-4"><div className="card kpi"><span className="kpi-label">Type</span><strong>{d.DocumentType}</strong></div><div className="card kpi"><span className="kpi-label">Version</span><strong>v{d.CurrentVersion}</strong></div><div className="card kpi"><span className="kpi-label">Confidentiality</span><strong>{d.Confidentiality}</strong></div><div className="card kpi"><span className="kpi-label">Status</span><strong>{d.Status}</strong></div></div><section className="card section"><h2>Storage reference</h2><p>{d.StorageProvider} · {d.OriginalFileName||'No filename'}</p>{d.StorageUrl?<a className="btn btn-secondary" href={d.StorageUrl} target="_blank" rel="noreferrer">Open controlled file</a>:<p className="muted">No physical storage URL has been registered yet.</p>}</section><section className="card section"><h2>Business links</h2><form action={addDocumentLink} className="form-grid"><input type="hidden" name="documentId" value={id}/><label>Entity type<input name="entityType" required/></label><label>Entity ID<input name="entityId" required/></label><label>Document/reference number<input name="entityNumber"/></label><label>Relationship<select name="relationshipType"><option>ATTACHMENT</option><option>EVIDENCE</option><option>SOURCE_DOCUMENT</option><option>SUPPORTING_DOCUMENT</option></select></label><div><button className="btn btn-primary">Add link</button></div></form><div className="table-wrap"><table className="table"><tbody>{x.links.map((l:any)=><tr key={l.Id}><td>{l.EntityType}</td><td>{l.EntityNumber||l.EntityId}</td><td>{l.RelationshipType}</td></tr>)}</tbody></table></div></section><section className="card section"><h2>Version history</h2><div className="table-wrap"><table className="table"><thead><tr><th>Version</th><th>File</th><th>Provider</th><th>Uploaded</th><th>Notes</th></tr></thead><tbody>{x.versions.map((v:any)=><tr key={v.Id}><td>v{v.VersionNumber}</td><td>{v.OriginalFileName||'—'}</td><td>{v.StorageProvider}</td><td>{String(v.UploadedAt).slice(0,16)}</td><td>{v.ChangeNotes}</td></tr>)}</tbody></table></div></section></main></AppShell>}