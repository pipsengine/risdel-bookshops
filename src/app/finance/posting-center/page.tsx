import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {glWorkspace} from "@/features/finance/gl-data";import {runAutomatedPosting} from "@/app/actions/general-ledger";
export default async function Page(){await requirePermission("finance.gl.view");const x=await glWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Posting Center</h1><p className="page-desc">Convert eligible operational transactions into balanced, idempotent double-entry journals.</p></div><form action={runAutomatedPosting}><button className="btn btn-primary">Run automated posting</button></form></div><FinanceTabs active="Posting Center"/><section className="grid grid-4"><div className="card stat"><span>Posted journals</span><strong>{x.journals.length}</strong></div><div className="card stat"><span>Posting runs</span><strong>{x.runs.length}</strong></div><div className="card stat"><span>Ledger entries</span><strong>{x.entries.length}</strong></div><div className="card stat"><span>Open periods</span><strong>{x.periods.filter((p:any)=>p.Status==="OPEN").length}</strong></div></section><section className="card section"><div className="section-head"><div><h2>Posting run history</h2><p>Each run scans supported source transactions but posts only sources that have not already been journalized.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Run</th><th>Started</th><th>Scanned</th><th>Posted</th><th>Errors</th><th>Status</th></tr></thead><tbody>{x.runs.length?x.runs.map((r:any)=><tr key={r.Id}><td><strong>{r.RunNumber}</strong></td><td>{r.StartedAt}</td><td>{r.SourcesScanned}</td><td>{r.JournalsPosted}</td><td>{r.Errors}</td><td><span className="badge badge-neutral">{r.Status}</span></td></tr>):<tr><td colSpan={6}>No posting runs yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
