import Link from "next/link";import { AppShell } from "@/components/app-shell";import { CustomerForm } from "@/components/customer-form";import { requirePermission } from "@/lib/authz";
export default async function Page(){await requirePermission("crm.customers.create");return <AppShell active="Customers"><main className="page crm-page"><div className="page-header"><div><Link href="/customers" className="back-link">← Customers</Link><h1 className="page-title">Add Customer</h1><p className="page-desc">Create an individual, household, school, reseller, corporate or institutional account.</p></div></div><section className="card section crm-form-card"><CustomerForm/></section></main></AppShell>}
