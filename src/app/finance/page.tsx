import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financeOverview} from "@/features/finance/data";const money=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.view");const x=await financeOverview();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Finance & Accounting</h1><p className="page-desc">Financial control foundation connecting sales, customers, procurement and cash operations.</p></div></div><FinanceTabs active="Overview"/><div className="grid-4"><div className="card kpi"><span>Accounts receivable</span><strong>{money(x.totalAR)}</strong></div><div className="card kpi"><span>Procurement commitments</span><strong>{money(x.totalAP)}</strong></div><div className="card kpi"><span>Cash / bank balance</span><strong>{money(x.cashBalance)}</strong></div><div className="card kpi"><span>Draft journals</span><strong>{x.openJournals}</strong></div></div><div className="document-detail-grid" style={{marginTop:16}}><section className="card section"><h2>Financial control</h2><div className="policy-list"><p>Sales invoices feed receivables without duplicating customer transaction history.</p><p>Purchase orders, GRNs and supplier credits provide the payables and three-way-match foundation.</p><p>Cashier settlements remain operational evidence and can be posted into cash/bank accounts.</p></div></section><section className="card section"><h2>Accounting foundation</h2><div className="policy-list"><p>Double-entry journals use a controlled chart of accounts.</p><p>Accounting periods support OPEN/CLOSED status for future period controls.</p><p>Google Sheets remains the active provider; IDs are migration-safe for a future relational database.</p></div></section></div></main></AppShell>}
