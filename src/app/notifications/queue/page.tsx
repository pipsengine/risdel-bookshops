import {AppShell} from "@/components/app-shell";import {NotificationTabs} from "@/components/notification-tabs";import {notificationWorkspace} from "@/features/notifications/data";import {requirePermission} from "@/lib/authz";import {queueCommunication} from "@/app/actions/notifications";export default async function Page(){await requirePermission("notifications.view");const w=await notificationWorkspace();return <AppShell active="Notifications"><main className="page"><div className="page-header"><div><h1 className="page-title">Communication Queue</h1><p className="page-desc">Compose in-app messages now; Email, SMS and WhatsApp remain provider-ready until configured.</p></div></div><NotificationTabs active="Communication Queue"/><div className="catalogue-layout"><section className="card section"><h2>Delivery queue</h2><div className="table-wrap"><table className="table"><thead><tr><th>Channel</th><th>Destination</th><th>Subject</th><th>Status</th><th>Attempts</th><th>Created</th></tr></thead><tbody>{w.queue.map((q:any)=><tr key={q.Id}><td>{q.Channel}</td><td>{q.Destination||"—"}</td><td>{q.Subject}</td><td><span className="badge badge-warning">{q.Status}</span></td><td>{q.Attempts}</td><td>{String(q.CreatedAt).slice(0,16).replace("T"," ")}</td></tr>)}{!w.queue.length&&<tr><td colSpan={6}>No external communications queued.</td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>New communication</h2><form action={queueCommunication} className="form-stack"><label className="field"><span>Recipient user</span><select name="userId"><option value="">Broadcast / no user</option>{w.users.map((u:any)=><option key={u.Id} value={u.Id}>{u.DisplayName} · {u.Email}</option>)}</select></label><label className="field"><span>Channel</span><select name="channel"><option>IN_APP</option><option>EMAIL</option><option>SMS</option><option>WHATSAPP</option></select></label><label className="field"><span>Destination</span><input name="destination" placeholder="Email or phone for external channels"/></label><label className="field"><span>Subject</span><input name="subject" required/></label><label className="field"><span>Message</span><textarea name="body" rows={5} required/></label><label className="field"><span>Priority</span><select name="priority"><option>NORMAL</option><option>WARNING</option><option>DANGER</option></select></label><button className="btn btn-primary">Queue communication</button></form></aside></div></main></AppShell>}
