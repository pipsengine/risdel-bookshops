import {AppShell} from "@/components/app-shell";import {NotificationTabs} from "@/components/notification-tabs";import {notificationWorkspace} from "@/features/notifications/data";import {requirePermission} from "@/lib/authz";export default async function Page(){await requirePermission("notifications.view");const w=await notificationWorkspace();return <AppShell active="Notifications"><main className="page"><div className="page-header"><div><h1 className="page-title">Business Alerts</h1><p className="page-desc">Cross-module exceptions and conditions that require operational attention.</p></div></div><NotificationTabs active="Business Alerts"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Severity</th><th>Alert</th><th>Message</th><th>Created</th></tr></thead><tbody>{w.alerts.map((a:any)=><tr key={a.Id}><td><span className={`badge ${a.Severity==="DANGER"?"badge-danger":"badge-warning"}`}>{a.Severity}</span></td><td><strong>{a.Title}</strong></td><td>{a.Message}</td><td>{String(a.CreatedAt).slice(0,16).replace("T"," ")}</td></tr>)}{!w.alerts.length&&<tr><td colSpan={4}>No active business alerts.</td></tr>}</tbody></table></div></section></main></AppShell>}
