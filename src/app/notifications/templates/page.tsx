import {AppShell} from "@/components/app-shell";import {NotificationTabs} from "@/components/notification-tabs";import {notificationWorkspace} from "@/features/notifications/data";import {requirePermission} from "@/lib/authz";import {createTemplate} from "@/app/actions/notifications";export default async function Page(){await requirePermission("notifications.view");const w=await notificationWorkspace();return <AppShell active="Notifications"><main className="page"><div className="page-header"><div><h1 className="page-title">Notification Templates</h1><p className="page-desc">Reusable business communication content with channel-specific delivery.</p></div></div><NotificationTabs active="Templates"/><div className="catalogue-layout"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Channel</th><th>Subject</th><th>System</th></tr></thead><tbody>{w.templates.map((x:any)=><tr key={x.Id}><td><strong>{x.Code}</strong></td><td>{x.Name}</td><td>{x.Channel}</td><td>{x.Subject}</td><td>{String(x.IsSystem)==="true"?"Yes":"No"}</td></tr>)}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Create template</h2><form action={createTemplate} className="form-stack"><label className="field"><span>Code</span><input name="code" required/></label><label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Channel</span><select name="channel"><option>IN_APP</option><option>EMAIL</option><option>SMS</option><option>WHATSAPP</option></select></label><label className="field"><span>Subject</span><input name="subject"/></label><label className="field"><span>Body</span><textarea name="body" rows={5} required/></label><button className="btn btn-primary">Create template</button></form></aside></div></main></AppShell>}
