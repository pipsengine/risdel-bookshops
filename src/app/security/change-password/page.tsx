import { AppShell } from "@/components/app-shell";import { ChangePasswordForm } from "@/components/change-password-form";import { requireSession } from "@/lib/authz";
export default async function Page(){const session=await requireSession();return <AppShell active="Security"><main className="page"><div className="page-header"><div><h1 className="page-title">Change password</h1><p className="page-desc">Protect your Risdel Books account with a strong, private password.</p></div></div>{session.mustChangePassword&&<div className="alert alert-warning" style={{marginBottom:18}}><strong>Password change required.</strong> This is your first sign in or an administrator reset your password. Change it before continuing normal work.</div>}<div className="card section" style={{maxWidth:620}}><h2>Account password</h2><p style={{marginBottom:20}}>Changing your password signs out existing database sessions for your account.</p><ChangePasswordForm/></div></main></AppShell>}
