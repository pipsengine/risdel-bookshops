import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {TaxTabs} from "@/components/tax-tabs";import {requirePermission} from "@/lib/authz";import {taxWorkspace} from "@/features/finance/tax-data";import {assignProductTax,createTaxCode} from "@/app/actions/tax";
export default async function Page(){await requirePermission("tax.configure");const x=await taxWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Tax Codes & Product Classification</h1><p className="page-desc">Maintain effective-dated tax rules and explicitly classify catalogue products.</p></div></div><FinanceTabs active="Tax & Compliance"/><TaxTabs active="Tax Codes"/><div className="document-detail-grid"><section className="card section"><h2>Tax codes</h2><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Name</th><th>Type</th><th>Rate</th><th>Treatment</th><th>Legal reference</th></tr></thead><tbody>{x.codes.map((r:any)=><tr key={r.Id}><td><strong>{r.Code}</strong></td><td>{r.Name}</td><td>{r.TaxType}</td><td>{Number(r.Rate||0)}%</td><td>{r.Treatment}</td><td>{r.LegalReference||'—'}</td></tr>)}</tbody></table></div><h3>Create tax code</h3><form action={createTaxCode} className="form-grid"><label>Code<input name="code" required/></label><label>Name<input name="name" required/></label><label>Tax type<select name="taxType"><option>VAT</option><option>WHT</option><option>OTHER</option></select></label><label>Rate %<input name="rate" type="number" step="0.01" min="0"/></label><label>Treatment<select name="treatment"><option>STANDARD</option><option>ZERO_RATED</option><option>EXEMPT</option><option>WITHHOLDING</option></select></label><label>Applies to<input name="appliesTo"/></label><label>Effective from<input name="effectiveFrom" type="date"/></label><label>Legal reference<input name="legalReference"/></label><label className="span-2">Description<input name="description"/></label><label><input name="recoverable" type="checkbox"/> Recoverable input tax</label><div><button className="btn btn-primary">Create code</button></div></form></section><aside className="card section"><h2>Product tax assignment</h2><form action={assignProductTax} className="form-grid"><label className="span-2">Product<select name="productId" required><option value="">Select product</option>{x.products.map((p:any)=><option key={p.Id} value={p.Id}>{p.Code} — {p.Name} [{p.TaxCode||'UNCLASSIFIED'}]</option>)}</select></label><label>Tax code<select name="taxCode"><option value="">Unclassified</option>{x.codes.map((c:any)=><option key={c.Id} value={c.Code}>{c.Code} — {c.Name}</option>)}</select></label><div><button className="btn btn-primary">Assign</button></div></form><div className="policy-list" style={{marginTop:18}}><p>Use <strong>VAT_ZERO_EDU</strong> only for qualifying educational books/materials.</p><p>Use <strong>VAT_STD_7_5</strong> for standard taxable supplies.</p><p>Do not classify uncertain items automatically; review their legal treatment first.</p></div></aside></div></main></AppShell>}
