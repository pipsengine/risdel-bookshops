import Link from "next/link";import {AppShell} from "@/components/app-shell";import {AcademicSalesTabs} from "@/components/academic-sales-tabs";import {requirePermission} from "@/lib/authz";import {academicSummary,listOrders,listSchools,money} from "@/features/academic-sales/data";
export default async function Page(){await requirePermission("academic.sales.view");const [s,schools,orders]=await Promise.all([academicSummary(),listSchools(),listOrders()]);return <AppShell active="Academic Sales"><main className="page academic-sales-page"><div className="page-header"><div><h1 className="page-title">Academic Sales</h1><p className="page-desc">Manage school relationships, approved book lists, parent/student fulfilment and institutional academic orders.</p></div><Link href="/academic-sales/orders" className="btn btn-primary">Create academic order</Link></div><AcademicSalesTabs active="Overview"/><div className="sales-kpis academic-kpis"><div className="card mini-kpi"><span>Partner schools</span><strong>{s.schools}</strong><small>Academic institutional profiles</small></div><div className="card mini-kpi"><span>Published / draft lists</span><strong>{s.lists}</strong><small>Class-specific requirements</small></div><div className="card mini-kpi"><span>Open orders</span><strong>{s.openOrders}</strong><small>{s.orders} total academic orders</small></div><div className="card mini-kpi"><span>Order value</span><strong>{money(s.orderValue)}</strong><small>Academic order pipeline</small></div></div><div className="academic-sales-grid"><section className="card section"><div className="section-heading"><div><h2>School portfolio</h2><p>Institutional accounts and commercial activity.</p></div><Link href="/academic-sales/schools">View all</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>School</th><th>Lists</th><th>Orders</th><th>Order value</th></tr></thead><tbody>{schools.slice(0,6).map(x=><tr key={x.Id}><td><strong>{x.CustomerName}</strong><span className="table-sub">{x.SchoolCode} · {x.SchoolType}</span></td><td>{x.BookListCount}</td><td>{x.OrderCount}</td><td>{money(x.OrderValue)}</td></tr>)}</tbody></table></div></section><section className="card section"><div className="section-heading"><div><h2>Stock attention</h2><p>Book-list fulfilment risks.</p></div><Link href="/academic-sales/availability">Availability</Link></div><div className="academic-attention"><div><strong>{s.outOfStock}</strong><span>Out of stock requirements</span></div><div><strong>{s.partial}</strong><span>Partially available requirements</span></div></div></section></div><section className="card section" style={{marginTop:16}}><div className="section-heading"><div><h2>Recent academic orders</h2><p>School and parent/student list fulfilment.</p></div><Link href="/academic-sales/orders">Order register</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>School</th><th>Type</th><th>Status</th><th>Total</th><th>Balance</th></tr></thead><tbody>{orders.slice(0,8).map(o=><tr key={o.Id}><td><strong>{o.OrderNumber}</strong><span className="table-sub">{o.StudentName||`${o.StudentCount||1} list(s)`}</span></td><td>{o.SchoolName}</td><td>{String(o.OrderType).replaceAll("_"," ")}</td><td><span className="badge badge-info">{o.Status}</span></td><td>{money(o.Total)}</td><td>{money(o.BalanceDue)}</td></tr>)}</tbody></table></div></section></main></AppShell>}
