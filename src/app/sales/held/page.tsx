import Link from "next/link";import { AppShell } from "@/components/app-shell";import { SalesTabs } from "@/components/sales-tabs";import { cancelHeld } from "@/app/actions/sales";import { heldSales,salesMoney } from "@/features/sales/data";import { requirePermission } from "@/lib/authz";
export default async function Page(){await requirePermission("sales.pos.hold");const rows=await heldSales();return <AppShell active="Sales"><main className="page sales-page"><div className="page-header"><div><h1 className="page-title">Held Sales</h1><p className="page-desc">Temporarily parked baskets that have not yet posted inventory or payment.</p></div></div><SalesTabs active="Held Sales"/><section className="card section" style={{marginTop:18}}><div className="table-wrap"><table className="table"><thead><tr><th>Reference</th><th>Customer</th><th>Cashier</th><th>Total</th><th>Held at</th><th>Status</th><th></th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.Id}><td><strong>{r.HoldNumber}</strong></td><td>{r.CustomerName}</td><td>{r.CashierName}</td><td>{salesMoney(r.Total)}</td><td>{new Date(r.CreatedAt).toLocaleString()}</td><td><span className="badge badge-warning">HELD</span></td><td><div style={{display:"flex",gap:6}}><Link href={`/sales/pos?held=${r.Id}`} className="btn btn-xs btn-primary">Resume</Link><form action={cancelHeld}><input type="hidden" name="id" value={r.Id}/><button className="btn btn-xs btn-secondary">Cancel</button></form></div></td></tr>):<tr><td colSpan={7}><div className="catalogue-empty"><strong>No held sales.</strong><span>Parked POS baskets will appear here.</span></div></td></tr>}</tbody></table></div></section></main></AppShell>}
