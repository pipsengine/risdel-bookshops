import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {glWorkspace} from "@/features/finance/gl-data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.trial-balance.view");const x=await glWorkspace(),balanced=Math.abs(x.totalDebit-x.totalCredit)<.01;return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Trial Balance</h1><p className="page-desc">Posted debit and credit totals by account for accounting control and reporting.</p></div><span className={`badge ${balanced?'badge-success':'badge-danger'}`}>{balanced?'Balanced':'Out of balance'}</span></div><FinanceTabs active="Trial Balance"/><section className="grid grid-2"><div className="card stat"><span>Total debits</span><strong>{m(x.totalDebit)}</strong></div><div className="card stat"><span>Total credits</span><strong>{m(x.totalCredit)}</strong></div></section><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Account</th><th>Type</th><th>Debit</th><th>Credit</th><th>Net balance</th></tr></thead><tbody>{x.trial.length?x.trial.map((a:any)=><tr key={a.Id}><td><strong>{a.AccountCode}</strong> — {a.AccountName}</td><td>{a.AccountType}</td><td>{m(a.Debit)}</td><td>{m(a.Credit)}</td><td>{m(a.Balance)}</td></tr>):<tr><td colSpan={5}>No posted balances yet.</td></tr>}</tbody><tfoot><tr><th colSpan={2}>TOTAL</th><th>{m(x.totalDebit)}</th><th>{m(x.totalCredit)}</th><th>{m(x.totalDebit-x.totalCredit)}</th></tr></tfoot></table></div></section></main></AppShell>}
