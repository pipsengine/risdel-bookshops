import {AppShell} from "@/components/app-shell";import {InventoryTabs} from "@/components/inventory-tabs";import {InventoryOperationForm} from "@/components/inventory-operation-form";import {requirePermission} from "@/lib/authz";import {inventoryLookups,listAdjustments} from "@/features/inventory/data";
export default async function Page(){await requirePermission('inventory.adjustments.view');const [lookups,rows]=await Promise.all([inventoryLookups(),listAdjustments()]);return <AppShell active="Inventory"><main className="page"><div className="page-header"><div><h1 className="page-title">Stock Adjustments</h1><p className="page-desc">Controlled corrections for physical count differences, losses and data corrections.</p></div></div><InventoryTabs active="Adjustments"/><div className="catalogue-layout"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Date</th><th>Product</th><th>Warehouse</th><th>Direction</th><th>Quantity</th><th>Reason</th><th>By</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.Id}><td>{new Date(r.CreatedAt).toLocaleString('en-NG')}</td><td><strong>{r.ProductName}</strong></td><td>{r.WarehouseName}</td><td><span className={`badge ${r.Quantity<0?'badge-danger':'badge-success'}`}>{r.Quantity<0?'Decrease':'Increase'}</span></td><td>{Math.abs(r.Quantity)}</td><td>{r.Reason}</td><td>{r.CreatedByName}</td></tr>):<tr><td colSpan={7}>No stock adjustments recorded.</td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Post adjustment</h2><p style={{marginBottom:18}}>Every adjustment requires a reason and is recorded in the audit and movement ledgers.</p><InventoryOperationForm mode="adjustment" lookups={lookups}/></aside></div></main></AppShell>}
