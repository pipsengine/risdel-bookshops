import {AppShell} from "@/components/app-shell";import {CustomerTabs} from "@/components/customer-tabs";import {LoyaltyTabs} from "@/components/loyalty-tabs";import {TierForm} from "@/components/loyalty-forms";import {requirePermission} from "@/lib/authz";import {loyaltyAdmin} from "@/features/customers/loyalty-data";
export default async function Page(){await requirePermission("crm.loyalty.view");const d=await loyaltyAdmin();return <AppShell active="Customers"><main className="page"><div className="page-header"><div><h1 className="page-title">Loyalty Tiers</h1><p className="page-desc">Define membership thresholds and tier benefits.</p></div></div><CustomerTabs active="Loyalty"/><LoyaltyTabs active="Tiers"/><div className="crm-two-col"><section className="card section"><h2>Configured tiers</h2><div className="table-wrap"><table className="table"><thead><tr><th>Tier</th><th>Min points</th><th>Multiplier</th><th>Discount</th></tr></thead><tbody>{d.tiers.map(x=><tr key={x.Id}><td><strong>{x.Name}</strong><span className="table-sub">{x.Code}</span></td><td>{Number(x.MinPoints||0).toLocaleString()}</td><td>{x.EarnMultiplier||1}×</td><td>{x.DiscountPercent||0}%</td></tr>)}</tbody></table></div></section><section className="card section"><h2>Create tier</h2><TierForm/></section></div></main></AppShell>}
