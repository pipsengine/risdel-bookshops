import {AppShell} from "@/components/app-shell";import {AcademicSalesTabs} from "@/components/academic-sales-tabs";import {BookListForm,BookListItemForm} from "@/components/academic-sales-forms";import {requirePermission} from "@/lib/authz";import {academicLookups,listBookLists,money} from "@/features/academic-sales/data";
export default async function Page(){await requirePermission("academic.sales.view");const [d,lists]=await Promise.all([academicLookups(),listBookLists()]);return <AppShell active="Academic Sales"><main className="page academic-sales-page"><div className="page-header"><div><h1 className="page-title">School Book Lists</h1><p className="page-desc">Create controlled school/session/class requirements and mark books as required, recommended or optional.</p></div><span className="badge badge-success">Academic master linked</span></div><AcademicSalesTabs active="Book Lists"/><div className="academic-sales-grid"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Book list</th><th>School</th><th>Session / Class</th><th>Items</th><th>Estimated value</th><th>Status</th></tr></thead><tbody>{lists.length?lists.map(l=><tr key={l.Id}><td><strong>{l.Name}</strong><span className="table-sub">{l.ListNumber} · v{l.Version}</span></td><td>{l.SchoolName}</td><td>{l.SessionName}<span className="table-sub">{l.ClassName}</span></td><td>{l.ItemCount}</td><td>{money(l.EstimatedTotal)}</td><td><span className="badge">{l.Status}</span></td></tr>):<tr><td colSpan={6}><div className="catalogue-empty"><strong>No school book lists yet.</strong><span>Create a list and then add catalogue items.</span></div></td></tr>}</tbody></table></div></section><aside className="academic-form-stack"><section className="card section"><h2>Create book list</h2><BookListForm schools={await listSchoolsSafe()} sessions={d.sessions} terms={d.terms} classes={d.classes}/></section><section className="card section"><h2>Add item</h2><BookListItemForm lists={lists} products={d.products}/></section></aside></div></main></AppShell>}
async function listSchoolsSafe(){const {listSchools}=await import("@/features/academic-sales/data");return listSchools()}
