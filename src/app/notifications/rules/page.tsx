import {AppShell} from "@/components/app-shell";import {NotificationTabs} from "@/components/notification-tabs";import {notificationWorkspace} from "@/features/notifications/data";import {requirePermission} from "@/lib/authz";import {createRule} from "@/app/actions/notifications";export default async function Page(){await requirePermission("notifications.view");const w=await notificationWorkspace();return <AppShell active="Notifications"><main className="page"><div className="page-header"><div><h1 className="page-title">Notification Rules</h1><p className="page-desc">Define which business events generate alerts, recipients and delivery channels.</p></div></div><NotificationTabs active="Rules"/><div className="catalogue-layout"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Rule</th><th>Module</th><th>Severity</th><th>Recipient</th><th>Channels</th><th>Cooldown</th></tr></thead><tbody>{w.rules.map((r:any)=><tr key={r.Id}><td><strong>{r.Name}</strong><span className="table-sub">{r.EventType}</span></td><td>{r.Module}</td><td>{r.Severity}</td><td>{r.RecipientType} {r.RecipientValue}</td><td>{r.Channels}</td><td>{r.CooldownMinutes} min</td></tr>)}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Create rule</h2><form action={createRule} className="form-stack"><label className="field"><span>Code</span><input name="code" required/></label><label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Event type</span><input name="eventType" required/></label><label className="field"><span>Module</span><input name="module" required/></label><label className="field"><span>Severity</span><select name="severity"><option>INFO</option><option>WARNING</option><option>DANGER</option></select></label><label className="field"><span>Recipient type</span><select name="recipientType"><option>ROLE</option><option>PERMISSION</option><option>USER</option></select></label><label className="field"><span>Recipient value</span><input name="recipientValue"/></label><label className="field"><span>Channels</span><input name="channels" defaultValue="IN_APP"/></label><button className="btn btn-primary">Create rule</button></form></aside></div></main></AppShell>}
