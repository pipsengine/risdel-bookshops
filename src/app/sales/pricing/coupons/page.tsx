import {AppShell} from "@/components/app-shell";import {SalesTabs} from "@/components/sales-tabs";import {CouponForm} from "@/components/pricing-forms";import {requirePermission} from "@/lib/authz";import {pricingContext} from "@/features/pricing/data";
export default async function Page(){await requirePermission("pricing.view");const c=await pricingContext();return <AppShell active="Sales"><main className="page sales-page"><div className="page-header"><div><h1 className="page-title">Coupons & Promo Codes</h1><p className="page-desc">Controlled campaign codes with minimum spend, usage caps and maximum-discount safeguards.</p></div></div><SalesTabs active="Pricing"/><div className="document-detail-grid"><section className="card section"><h3>Create coupon</h3><CouponForm/></section><section className="card section"><h3>Coupon controls</h3><div className="policy-list"><p>Usage limits are stored independently from promotional rules.</p><p>Per-customer limits provide a foundation for CRM-linked redemption controls.</p><p>Minimum-spend and maximum-discount values protect promotional margins.</p></div></section></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Discount</th><th>Minimum spend</th><th>Usage</th><th>Status</th></tr></thead><tbody>{c.coupons.map((x:any)=><tr key={x.Id}><td><strong>{x.Code}</strong><span className="table-sub">{x.Name}</span></td><td>{x.DiscountType==='PERCENT'?`${x.DiscountValue}%`:`₦${Number(x.DiscountValue||0).toLocaleString()}`}</td><td>₦{Number(x.MinSpend||0).toLocaleString()}</td><td>{x.UsageCount||0} / {x.UsageLimit||'∞'}</td><td><span className="badge badge-success">{x.Status}</span></td></tr>)}</tbody></table></div></section></main></AppShell>}
