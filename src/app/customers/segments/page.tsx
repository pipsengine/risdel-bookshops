import { AppShell } from "@/components/app-shell";import { CustomerTabs } from "@/components/customer-tabs";import { requirePermission } from "@/lib/authz";import { listSegments } from "@/features/customers/data";import { createSegment } from "@/app/actions/customers";import { SegmentForm } from "@/components/segment-form";
export default async function Page(){await requirePermission("crm.customers.view");const rows=await listSegments();return <AppShell active="Customers"><main className="page crm-page"><div className="page-header"><div><h1 className="page-title">Customer Segments</h1><p className="page-desc">Build reusable groups for schools, VIPs, resellers, campaigns and relationship management.</p></div></div><CustomerTabs active="Segments"/><div className="crm-two-col"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Segment</th><th>Code</th><th>Type</th><th>Customers</th></tr></thead><tbody>{rows.map(s=><tr key={s.Id}><td><strong>{s.Name}</strong><span className="table-sub">{s.Description||""}</span></td><td>{s.Code}</td><td>{s.SegmentType}</td><td>{s.CustomerCount}</td></tr>)}{!rows.length&&<tr><td colSpan={4}>No segments yet.</td></tr>}</tbody></table></div></section><section className="card section"><h3>Create segment</h3><SegmentForm/></section></div></main></AppShell>}
