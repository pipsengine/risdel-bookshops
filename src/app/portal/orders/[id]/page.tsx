import {redirect,notFound} from "next/navigation";import {StorefrontHeader} from "@/components/storefront-header";import {portalSession,money} from "@/features/commerce/data";import {findById,getRows,SHEETS} from "@/lib/sheets";
export default async function Page({params}:{params:Promise<{id:string}>}){const s=await portalSession();if(!s?.customer)redirect("/portal/login");const {id}=await params,o:any=await findById(SHEETS.commerceOrders,id);if(!o||o.CustomerId!==s.customer.Id)notFound();const lines=(await getRows(SHEETS.commerceOrderLines)).filter(l=>l.OrderId===id);return <div className="store-shell"><StorefrontHeader/><main className="store-main narrow-store"><span className="eyebrow">ORDER {o.OrderNumber}</span><h1>Order details</h1><section className="store-panel"><div className="order-meta"><div><small>Status</small><strong>{o.Status}</strong></div><div><small>Payment</small><strong>{o.PaymentStatus}</strong></div><div><small>Fulfilment</small><strong>{o.FulfilmentMode}</strong></div><div><small>Total</small><strong>{money(Number(o.Total||0))}</strong></div></div>{lines.map((l:any)=><div className="order-line" key={l.Id}><span>{l.Quantity} × {l.ProductName}</span><b>{money(Number(l.LineTotal||0))}</b></div>)}</section></main></div>}
