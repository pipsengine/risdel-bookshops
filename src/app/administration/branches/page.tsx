import Link from "next/link";import { AppShell } from "@/components/app-shell";import { OrganisationTabs } from "@/components/organisation-tabs";import { BranchForm } from "@/components/branch-form";import { requirePermission } from "@/lib/authz";import { getPrimaryCompany,listBranches,organisationUsers } from "@/features/organisation/data";import { setHeadOffice,toggleBranch } from "@/app/actions/organisation";
export default async function Page(){await requirePermission('organisation.branches.view');const [company,branches,users]=await Promise.all([getPrimaryCompany(),listBranches(),organisationUsers()]);return <AppShell active="Organisation"><main className="page"><div className="page-header"><div><h1 className="page-title">Branches</h1><p className="page-desc">Manage Risdel Enterprise stores, offices and future fulfilment centres.</p></div><span className="badge badge-info">{branches.filter((b:any)=>b.IsActive).length} active</span></div><OrganisationTabs active="Branches"/><div className="admin-split organisation-split" style={{marginTop:18}}><section className="card section"><div className="section-heading"><div><h2>Branch directory</h2><p>Every transaction will inherit company and branch context as operational modules are added.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Branch</th><th>Manager</th><th>Warehouses</th><th>Status</th><th></th></tr></thead><tbody>{branches.map((b:any)=><tr key={String(b.Id)}><td><div className="identity-cell"><div className="location-mark mini">{String(b.Code).slice(0,2)}</div><div><Link href={`/administration/branches/${b.Id}`}><strong>{b.Name}</strong><small>{b.Code} · {[b.City,b.State].filter(Boolean).join(', ')||'Location pending'}</small></Link></div></div></td><td>{b.ManagerName||<span className="muted">Unassigned</span>}</td><td>{b.WarehouseCount}</td><td><div className="status-stack"><span className={`badge ${b.IsActive?'badge-success':'badge-neutral'}`}>{b.IsHeadOffice?'Head Office':b.IsActive?'Active':'Inactive'}</span></div></td><td><div className="row-actions">{!b.IsHeadOffice&&b.IsActive&&<form action={setHeadOffice}><input type="hidden" name="id" value={String(b.Id)}/><button className="btn btn-xs btn-secondary">Set head office</button></form>}<form action={toggleBranch}><input type="hidden" name="id" value={String(b.Id)}/><input type="hidden" name="active" value={String(!b.IsActive)}/><button disabled={Boolean(b.IsHeadOffice&&b.IsActive)} className="btn btn-xs btn-secondary">{b.IsActive?'Deactivate':'Activate'}</button></form></div></td></tr>)}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Add branch</h2><p style={{marginBottom:18}}>Create another store, office or fulfilment location without changing the existing company structure.</p>{company?<BranchForm companyId={String(company.Id)} users={users}/>:<div className="alert alert-warning">Company setup is required first.</div>}</aside></div></main></AppShell>}
