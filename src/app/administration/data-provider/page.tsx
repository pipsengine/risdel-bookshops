import { AppShell } from "@/components/app-shell";import { AdminTabs } from "@/components/admin-tabs";import { requirePermission } from "@/lib/authz";import { dataProviderName } from "@/lib/data-provider";import { persistenceHealth } from "@/lib/persistence-health";import { SHEETS,getRows } from "@/lib/sheets";
export default async function Page(){await requirePermission("admin.data-provider.view");const h=await persistenceHealth();let sheetCount:number|null=null;if(dataProviderName()==="google-sheets"&&h.status==="Healthy"){let ok=0;for(const name of Object.values(SHEETS))try{await getRows(name);ok++}catch{}sheetCount=ok}return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">Data provider</h1><p className="page-desc">Read-only diagnostics for the active Risdel Bookshops persistence provider.</p></div><span className={`badge ${h.status==='Healthy'?'badge-success':'badge-warning'}`}>{h.status}</span></div><AdminTabs active="Data Provider"/><div className="system-grid" style={{marginTop:18}}><section className="card section"><h2>Current provider</h2><div className="setting-row"><span>Provider</span><strong>{dataProviderName()==='google-sheets'?'Google Sheets':'SQL Server'}</strong></div><div className="setting-row"><span>Connection</span><strong>{h.status}</strong></div><div className="setting-row"><span>Required Google worksheets</span><strong>{sheetCount==null?'Not applicable':`${sheetCount} / ${Object.keys(SHEETS).length}`}</strong></div></section><section className="card section"><h2>Security</h2><p>Credentials are loaded only from server-side environment variables and are never rendered here.</p><div className="note" style={{marginTop:14}}>The Google spreadsheet must remain Restricted and shared explicitly with the service-account email as Editor.</div></section></div><section className="card section" style={{marginTop:18}}><h2>Connection detail</h2><p>{h.detail}</p></section></main></AppShell>}
