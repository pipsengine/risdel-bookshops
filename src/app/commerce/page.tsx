import Link from "next/link";import {AppShell} from "@/components/app-shell";import {CommerceTabs} from "@/components/commerce-tabs";import {requirePermission} from "@/lib/authz";import {commerceAdminSummary,money} from "@/features/commerce/data";import {updateCommerceOrderStatus,updateCommercePayment} from "@/app/actions/commerce";
export default async function Page(){await requirePermission("commerce.view");const x=await commerceAdminSummary();return <AppShell active="Online Store"><main className="page"><div className="page-header"><div><h1 className="page-title">E-Commerce & Online Orders</h1><p className="page-desc">Customer portal, web orders, payment status and online fulfilment handoff.</p></div><Link className="btn btn-secondary" href="/shop" target="_blank">Open storefront ↗</Link></div><CommerceTabs active="Overview"/><div className="grid-4" style={{marginTop:18}}><div className="card kpi"><span className="kpi-label">Online orders</span><strong className="kpi-value">{x.orders.length}</strong></div><div className="card kpi"><span className="kpi-label">Pending action</span><strong className="kpi-value">{x.pending}</strong></div><div className="card kpi"><span className="kpi-label">Portal accounts</span><strong className="kpi-value">{x.portalAccounts}</strong></div><div className="card kpi"><span className="kpi-label">Order value</span><strong>{money(x.revenue)}</strong></div></div><section className="card section" style={{marginTop:18}}><h2>Online order register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Mode</th><th>Payment</th><th>Status</th><th>Total</th><th>Action</th></tr></thead><tbody>{x.orders.slice(0,100).map((o:any)=><tr key={o.Id}><td>{o.OrderNumber}<span className="table-sub">{String(o.CreatedAt).slice(0,16)}</span></td><td>{o.CustomerName}<span className="table-sub">{o.Email}</span></td><td>{o.FulfilmentMode}</td><td>{o.PaymentStatus}<span className="table-sub">{o.PaymentMethod}</span></td><td><span className="badge badge-neutral">{o.Status}</span></td><td>{money(Number(o.Total||0))}</td><td><form action={updateCommerceOrderStatus} className="inline-form"><input type="hidden" name="id" value={o.Id}/><select className="input" name="status" defaultValue={o.Status}><option>AWAITING_PAYMENT</option><option>CONFIRMED</option><option>PROCESSING</option><option>READY</option><option>FULFILLED</option><option>CANCELLED</option></select><button className="btn btn-secondary btn-sm">Update</button></form><form action={updateCommercePayment} className="inline-form" style={{marginTop:6}}><input type="hidden" name="orderId" value={o.Id}/><select className="input" name="paymentStatus" defaultValue={o.PaymentStatus}><option>PENDING</option><option>PAID</option><option>FAILED</option><option>REFUNDED</option></select><input className="input" name="reference" placeholder="Payment ref"/><button className="btn btn-secondary btn-sm">Payment</button></form></td></tr>)}</tbody></table></div></section></main></AppShell>}
