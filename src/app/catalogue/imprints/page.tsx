import {AppShell} from "@/components/app-shell";import {CatalogueTabs} from "@/components/catalogue-tabs";import {ImprintForm} from "@/components/imprint-form";import {requirePermission} from "@/lib/authz";import {publisherImprints} from "@/features/catalogue/academic-data";import {listMaster} from "@/features/catalogue/data";
export default async function Page(){await requirePermission("catalogue.master.view");const [rows,publishers]=await Promise.all([publisherImprints(),listMaster("cataloguePublishers")]);return <AppShell active="Catalogue"><main className="page catalogue-page"><div className="page-header"><div><h1 className="page-title">Publisher imprints</h1><p className="page-desc">Maintain publishing labels under their legal/primary publisher for accurate book metadata and supplier reporting.</p></div><span className="badge badge-info">{rows.filter((x:any)=>x.IsActive).length} active</span></div><CatalogueTabs active="Imprints"/><div className="catalogue-layout master-layout"><section className="card section"><div className="section-heading"><div><h2>Imprint directory</h2><p>Imprints remain linked to a publisher instead of being duplicated as publishers.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Imprint</th><th>Publisher</th><th>Code</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map((x:any)=><tr key={x.Id}><td><strong>{x.Name}</strong></td><td>{x.PublisherName}</td><td>{x.Code}</td><td><span className={`badge ${x.IsActive?"badge-success":"badge-neutral"}`}>{x.IsActive?"Active":"Inactive"}</span></td></tr>):<tr><td colSpan={4}><div className="catalogue-empty"><strong>No imprints yet.</strong><span>Create an imprint only when a publisher operates distinct publishing labels.</span></div></td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Add imprint</h2><p style={{marginBottom:18}}>Link the imprint to its publisher master record.</p><ImprintForm publishers={publishers.filter((x:any)=>x.IsActive)}/></aside></div></main></AppShell>}
