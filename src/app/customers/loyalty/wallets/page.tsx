import {AppShell} from "@/components/app-shell";import {CustomerTabs} from "@/components/customer-tabs";import {LoyaltyTabs} from "@/components/loyalty-tabs";import {WalletForm} from "@/components/loyalty-forms";import {requirePermission} from "@/lib/authz";import {loyaltyAdmin,loyaltyMoney} from "@/features/customers/loyalty-data";
export default async function Page(){await requirePermission("crm.loyalty.view");const d=await loyaltyAdmin();return <AppShell active="Customers"><main className="page"><div className="page-header"><div><h1 className="page-title">Customer Wallets</h1><p className="page-desc">Track promotional/store-credit balances separately from loyalty points.</p></div></div><CustomerTabs active="Loyalty"/><LoyaltyTabs active="Wallets"/><div className="crm-two-col"><section className="card section"><h2>Wallet balances</h2><div className="table-wrap"><table className="table"><thead><tr><th>Customer</th><th>Balance</th><th>Status</th></tr></thead><tbody>{d.wallets.map(w=><tr key={w.Id}><td>{d.customers.find(c=>c.Id===w.CustomerId)?.Name||"Unknown"}</td><td><strong>{loyaltyMoney(Number(w.Balance||0))}</strong></td><td>{w.Status}</td></tr>)}{!d.wallets.length&&<tr><td colSpan={3}>No customer wallets yet.</td></tr>}</tbody></table></div></section><WalletForm customers={d.customers}/></div></main></AppShell>}
