import Link from "next/link";import {AppShell} from "@/components/app-shell";import {DocumentsTabs} from "@/components/documents-tabs";import {requirePermission} from "@/lib/authz";import {documentCentre} from "@/features/documents/data";import {createDocument} from "@/app/actions/documents";export default async function Page(){await requirePermission("documents.view");const x=await documentCentre();return <AppShell active="Documents"><main className="page"><div className="page-header"><div><h1 className="page-title">Documents & Attachments Centre</h1><p className="page-desc">Controlled document register, entity attachments, versions and storage references across Risdel Bookshops.</p></div></div><DocumentsTabs active="Document Centre"/><div className="grid-4"><div className="card kpi"><span className="kpi-label">Documents</span><strong className="kpi-value">{x.total}</strong></div><div className="card kpi"><span className="kpi-label">Active</span><strong className="kpi-value">{x.active}</strong></div><div className="card kpi"><span className="kpi-label">Confidential</span><strong className="kpi-value">{x.confidential}</strong></div><div className="card kpi"><span className="kpi-label">Archived</span><strong className="kpi-value">{x.archived}</strong></div></div><section className="card section"><h2>Register document</h2><form action={createDocument} className="form-grid"><label>Title<input name="title" required/></label><label>Type<select name="documentType"><option>OTHER</option><option>INVOICE</option><option>RECEIPT</option><option>PURCHASE_ORDER</option><option>GRN</option><option>CONTRACT</option><option>EVIDENCE</option></select></label><label>Category<input name="category" placeholder="Finance, Supplier, Customer..."/></label><label>Confidentiality<select name="confidentiality"><option>INTERNAL</option><option>PUBLIC</option><option>CONFIDENTIAL</option><option>RESTRICTED</option></select></label><label>Original filename<input name="fileName"/></label><label>Storage provider<select name="storageProvider"><option>REFERENCE</option><option>GOOGLE_DRIVE</option><option>LOCAL</option><option>SHAREPOINT</option><option>OBJECT_STORAGE</option></select></label><label className="span-2">Storage URL<input name="storageUrl" placeholder="Provider URL or controlled file reference"/></label><label>Entity type<input name="entityType" placeholder="SUPPLIER_INVOICE"/></label><label>Entity ID<input name="entityId"/></label><label className="span-2">Description<textarea name="description"/></label><div><button className="btn btn-primary">Register document</button></div></form></section><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Document</th><th>Type</th><th>Category</th><th>Version</th><th>Confidentiality</th><th>Status</th><th>Updated</th></tr></thead><tbody>{x.rows.length?x.rows.map((d:any)=><tr key={d.Id}><td><Link className="back-link" href={`/documents/${d.Id}`}>{d.DocumentNumber}</Link><span className="table-sub">{d.Title}</span></td><td>{d.DocumentType}</td><td>{d.Category}</td><td>v{d.CurrentVersion||d.Versions||1}</td><td>{d.Confidentiality}</td><td><span className={`badge ${d.Status==='ARCHIVED'?'':'badge-success'}`}>{d.Status}</span></td><td>{String(d.UpdatedAt||d.CreatedAt).slice(0,10)}</td></tr>):<tr><td colSpan={7}>No documents registered yet.</td></tr>}</tbody></table></div></section></main></AppShell>}