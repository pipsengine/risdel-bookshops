import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {TaxTabs} from "@/components/tax-tabs";import {requirePermission} from "@/lib/authz";import {money,taxWorkspace} from "@/features/finance/tax-data";import {createVatReturn} from "@/app/actions/tax";
export default async function Page(){await requirePermission("tax.reports.view");const x=await taxWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">VAT & WHT Reports</h1><p className="page-desc">Working tax registers and return preparation; figures remain reviewable before filing.</p></div></div><FinanceTabs active="Tax & Compliance"/><TaxTabs active="VAT & WHT Reports"/><div className="grid-4"><div className="card kpi"><span>Output VAT</span><strong>{money(x.outputVAT)}</strong></div><div className="card kpi"><span>Input VAT</span><strong>{money(x.inputVAT)}</strong></div><div className="card kpi"><span>Net VAT estimate</span><strong>{money(x.netVAT)}</strong></div><div className="card kpi"><span>WHT outstanding</span><strong>{money(x.whtOutstanding)}</strong></div></div><div className="document-detail-grid" style={{marginTop:16}}><section className="card section"><h2>VAT return register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Return</th><th>Period</th><th>Output VAT</th><th>Input VAT</th><th>Net VAT</th><th>Status</th></tr></thead><tbody>{x.vatReturns.length?x.vatReturns.map((r:any)=><tr key={r.Id}><td>{r.ReturnNumber}</td><td>{r.PeriodStart} → {r.PeriodEnd}</td><td>{money(r.OutputVAT)}</td><td>{money(r.InputVAT)}</td><td>{money(r.NetVAT)}</td><td>{r.Status}</td></tr>):<tr><td colSpan={6}>No VAT return prepared yet.</td></tr>}</tbody></table></div></section><aside className="card section"><h2>Prepare draft VAT return</h2><form action={createVatReturn} className="form-grid"><label className="span-2">Accounting period<select name="periodId" required><option value="">Select</option>{x.periods.map((p:any)=><option key={p.Id} value={p.Id}>{p.PeriodCode} — {p.Name}</option>)}</select></label><label>Output VAT<input name="outputVAT" type="number" step="0.01" defaultValue={x.outputVAT}/></label><label>Input VAT<input name="inputVAT" type="number" step="0.01" defaultValue={x.inputVAT}/></label><label>Net VAT<input name="netVAT" type="number" step="0.01" defaultValue={x.netVAT}/></label><label>Taxable sales<input name="taxableSales" type="number" step="0.01"/></label><label>Zero-rated sales<input name="zeroRatedSales" type="number" step="0.01"/></label><label>Exempt sales<input name="exemptSales" type="number" step="0.01"/></label><label className="span-2">Notes<input name="notes"/></label><button className="btn btn-primary">Prepare draft</button></form></aside></div><section className="card section" style={{marginTop:16}}><h2>WHT register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Transaction</th><th>Supplier</th><th>Taxable base</th><th>Rate</th><th>Amount</th><th>Status</th></tr></thead><tbody>{x.wht.length?x.wht.map((r:any)=><tr key={r.Id}><td>{r.TransactionNumber}</td><td>{x.suppliers.find((s:any)=>s.Id===r.SupplierId)?.Name||'Supplier'}</td><td>{money(r.TaxableBase)}</td><td>{Number(r.Rate||0)}%</td><td>{money(r.Amount)}</td><td>{r.Status}</td></tr>):<tr><td colSpan={6}>No WHT transactions recorded. WHT automation remains disabled until explicit rules are configured.</td></tr>}</tbody></table></div></section></main></AppShell>}
