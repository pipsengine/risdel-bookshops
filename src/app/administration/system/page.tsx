import Link from "next/link";import { AppShell } from "@/components/app-shell";import { AdminTabs } from "@/components/admin-tabs";import { appConfig } from "@/config/app";import { persistenceHealth } from "@/lib/persistence-health";import { dataProviderName } from "@/lib/data-provider";
export default async function SystemInfo(){const p=await persistenceHealth();return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">System information</h1><p className="page-desc">Environment, platform configuration and persistence health.</p></div><span className={`badge ${p.status==='Healthy'?'badge-success':'badge-warning'}`}>{p.status}</span></div><AdminTabs active="System"/><div className="system-grid" style={{marginTop:18}}><section className="card section"><h2>Application</h2><p>Current application identity and runtime.</p><div style={{marginTop:14}}><div className="setting-row"><span>System</span><strong>{appConfig.name}</strong></div><div className="setting-row"><span>Company</span><strong>{appConfig.company}</strong></div><div className="setting-row"><span>Version</span><strong>{appConfig.version}</strong></div><div className="setting-row"><span>Environment</span><strong style={{textTransform:'capitalize'}}>{appConfig.environment}</strong></div><div className="setting-row"><span>Timezone</span><strong>{appConfig.timezone}</strong></div><div className="setting-row"><span>Currency</span><strong>{appConfig.currency}</strong></div></div></section><section className="card section"><h2>Data provider</h2><p>Persistence is abstracted so the provider can be migrated later.</p><div style={{marginTop:14}}><div className="setting-row"><span>Current provider</span><strong>{dataProviderName()==='google-sheets'?'Google Sheets':'SQL Server'}</strong></div><div className="setting-row"><span>Provider status</span><strong><span className={`badge ${p.status==='Healthy'?'badge-success':'badge-warning'}`}>{p.status}</span></strong></div><div className="setting-row"><span>Storage</span><strong><span className="badge badge-success">Ready</span></strong></div></div><Link className="btn btn-secondary full-button" href="/administration/data-provider">Data provider diagnostics</Link></section></div><section className="card section" style={{marginTop:18}}><h2>Persistence connection detail</h2><p>{p.detail}</p></section></main></AppShell>}
