import {AppShell} from "@/components/app-shell";import {DocumentsTabs} from "@/components/documents-tabs";import {requirePermission} from "@/lib/authz";import {documentCentre} from "@/features/documents/data";export default async function Page(){const u=await requirePermission("documents.view"),x=await documentCentre(),rows=x.rows.filter((d:any)=>d.OwnerUserId===u.userId||d.UploadedBy===u.userId);return <AppShell active="Documents"><main className="page"><div className="page-header"><div><h1 className="page-title">My Documents</h1><p className="page-desc">Documents you own or registered.</p></div></div><DocumentsTabs active="My Documents"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Number</th><th>Title</th><th>Type</th><th>Status</th></tr></thead><tbody>{rows.map((d:any)=><tr key={d.Id}><td>{d.DocumentNumber}</td><td>{d.Title}</td><td>{d.DocumentType}</td><td>{d.Status}</td></tr>)}</tbody></table></div></section></main></AppShell>}