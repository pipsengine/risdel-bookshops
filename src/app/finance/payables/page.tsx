import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financeOverview} from "@/features/finance/data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.payables.view");const x=await financeOverview();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Accounts Payable</h1><p className="page-desc">Supplier commitments, credits and matching readiness.</p></div></div><FinanceTabs active="Payables"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>PO</th><th>Supplier</th><th>Total</th><th>Credits</th><th>Net commitment</th><th>Status</th></tr></thead><tbody>{x.payables.map((r:any)=><tr key={r.Id}><td><strong>{r.PONumber||r.PurchaseOrderNumber}</strong></td><td>{r.SupplierName}</td><td>{m(r.Total)}</td><td>{m(r.Credits)}</td><td>{m(Number(r.Total||0)-Number(r.Credits||0))}</td><td><span className="badge badge-neutral">{r.Status}</span></td></tr>)}</tbody></table></div></section></main></AppShell>}
