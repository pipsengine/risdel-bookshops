import {AppShell} from "@/components/app-shell";import {AcademicSalesTabs} from "@/components/academic-sales-tabs";import {SchoolForm} from "@/components/academic-sales-forms";import {requirePermission} from "@/lib/authz";import {academicLookups,listSchools,money} from "@/features/academic-sales/data";import {getRows,SHEETS} from "@/lib/sheets";
export default async function Page(){await requirePermission("academic.sales.view");const [d,schools,curricula]=await Promise.all([academicLookups(),listSchools(),getRows(SHEETS.catalogueCurricula)]);return <AppShell active="Academic Sales"><main className="page academic-sales-page"><div className="page-header"><div><h1 className="page-title">Schools & Institutions</h1><p className="page-desc">Extend existing SCHOOL customer accounts with academic-sales configuration and portfolio metrics.</p></div><span className="badge badge-info">{schools.length} schools</span></div><AcademicSalesTabs active="Schools"/><div className="academic-sales-grid"><section className="card section"><div className="section-heading"><div><h2>School portfolio</h2><p>Each school remains linked to one CRM customer account.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>School</th><th>Type</th><th>Population</th><th>Book lists</th><th>Orders</th><th>Order value</th></tr></thead><tbody>{schools.length?schools.map(s=><tr key={s.Id}><td><strong>{s.CustomerName}</strong><span className="table-sub">{s.SchoolCode} · {s.Email||s.Phone||"No contact"}</span></td><td>{s.SchoolType}</td><td>{Number(s.StudentPopulation||0).toLocaleString()}</td><td>{s.BookListCount}</td><td>{s.OrderCount}</td><td>{money(s.OrderValue)}</td></tr>):<tr><td colSpan={6}><div className="catalogue-empty"><strong>No academic school profiles yet.</strong><span>Create a SCHOOL customer first, then extend it here.</span></div></td></tr>}</tbody></table></div></section><aside className="card section"><h2>Create school profile</h2><p className="section-copy">Only CRM customers classified as SCHOOL are listed.</p><SchoolForm customers={d.customers} curricula={curricula.filter((x:any)=>String(x.IsActive??true)!=="false")}/></aside></div></main></AppShell>}
