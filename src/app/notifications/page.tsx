import Link from "next/link";import {AppShell} from "@/components/app-shell";import {NotificationTabs} from "@/components/notification-tabs";import {notificationWorkspace} from "@/features/notifications/data";import {requirePermission} from "@/lib/authz";import {readNotification} from "@/app/actions/notifications";
export default async function Page(){const u=await requirePermission("notifications.view"),w=await notificationWorkspace(u.userId||undefined);return <AppShell active="Notifications"><main className="page"><div className="page-header"><div><h1 className="page-title">Notifications & Communication Centre</h1><p className="page-desc">Operational alerts, personal notifications and multi-channel communications in one controlled workspace.</p></div><span className="badge badge-success">Module 26 · Live</span></div><NotificationTabs active="Centre"/><div className="sales-kpis"><div className="card mini-kpi"><span>Unread</span><strong>{w.summary.unread}</strong><small>Needs your attention</small></div><div className="card mini-kpi"><span>Critical</span><strong>{w.summary.critical}</strong><small>High-severity notifications</small></div><div className="card mini-kpi"><span>Queued</span><strong>{w.summary.queued}</strong><small>External communications</small></div><div className="card mini-kpi"><span>Failed</span><strong>{w.summary.failed}</strong><small>Delivery retry required</small></div></div><section className="card section"><div className="section-title-row"><div><h2>My notifications</h2><p>Latest business events assigned to you or broadcast across Risdel Bookshops.</p></div><Link className="btn" href="/notifications/queue">Send communication</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Notification</th><th>Type</th><th>Priority</th><th>Created</th><th>Status</th><th></th></tr></thead><tbody>{w.notifications.slice(0,40).map((n:any)=><tr key={n.Id}><td><strong>{n.Title}</strong><span className="table-sub">{n.Message}</span></td><td>{n.Type}</td><td><span className={`badge ${["DANGER","CRITICAL"].includes(String(n.Priority))?"badge-danger":n.Priority==="WARNING"?"badge-warning":"badge-neutral"}`}>{n.Priority||"NORMAL"}</span></td><td>{String(n.CreatedAt).slice(0,16).replace("T"," ")}</td><td>{String(n.IsRead)==="true"?<span className="badge badge-success">READ</span>:<span className="badge badge-warning">UNREAD</span>}</td><td>{!n.IsRead&&<form action={readNotification}><input type="hidden" name="id" value={n.Id}/><button className="btn btn-xs">Mark read</button></form>}</td></tr>)}{!w.notifications.length&&<tr><td colSpan={6}>No notifications yet.</td></tr>}</tbody></table></div></section></main></AppShell>}
