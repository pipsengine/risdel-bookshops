import { AppShell } from "@/components/app-shell";import { appConfig } from "@/config/app";import { databaseHealth } from "@/lib/db";
export default async function Dashboard(){const db=await databaseHealth();return <AppShell><main className="page"><div className="page-header"><div><h1 className="page-title">Good morning</h1><p className="page-desc">Welcome to {appConfig.name}. Module 00 has established the production foundation.</p></div><span className="badge badge-success">Foundation v{appConfig.version}</span></div><section className="grid grid-4"><div className="card kpi"><div className="kpi-label">System foundation</div><div className="kpi-value">100%</div><div className="kpi-meta">Repository, architecture & UI shell</div></div><div className="card kpi"><div className="kpi-label">Environment</div><div className="kpi-value" style={{fontSize:22,textTransform:"capitalize"}}>{appConfig.environment}</div><div className="kpi-meta">External configuration enabled</div></div><div className="card kpi"><div className="kpi-label">Database</div><div className="kpi-value" style={{fontSize:22}}>{db.status}</div><div className="kpi-meta">Microsoft SQL Server</div></div><div className="card kpi"><div className="kpi-label">Active branch</div><div className="kpi-value" style={{fontSize:22}}>{appConfig.branch}</div><div className="kpi-meta">Multi-branch architecture ready</div></div></section><div className="grid grid-3" style={{marginTop:18}}><section className="card section" style={{gridColumn:"span 2"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:10}}><div><h2>Implementation progress</h2><p>One evolving repository. Each future module updates this codebase.</p></div><span className="badge badge-info">MODULE 00</span></div><div className="progress-list">{[["Project foundation","Repository, TypeScript, configuration","Complete"],["Security foundation","Session security, protected routes, headers","Complete"],["Database foundation","SQL Server schema, migrations and seed scripts","Complete"],["Users, roles & permissions","Full administration and RBAC UI","Next"],["Catalogue & inventory","Books, products and stock operations","Planned"],["Sales & POS","Retail transactions and cashier operations","Planned"]].map(([a,b,c])=><div className="progress-row" key={a}><div><div className="progress-title">{a}</div><div className="progress-sub">{b}</div></div><div></div><span className={`badge ${c==='Complete'?'badge-success':c==='Next'?'badge-warning':'badge-neutral'}`}>{c}</span></div>)}</div></section><section className="card section"><h2>System health</h2><p>Core service readiness for this environment.</p><div style={{marginTop:16}}><div className="setting-row"><span>Application</span><strong className="health"><i className="health-dot"/>Healthy</strong></div><div className="setting-row"><span>Database</span><strong className="health"><i className={`health-dot ${db.status==='Healthy'?'':'warn'}`}/>{db.status}</strong></div><div className="setting-row"><span>File storage</span><strong className="health"><i className="health-dot"/>Ready</strong></div><div className="setting-row"><span>Version</span><strong>{appConfig.version}</strong></div></div></section></div><section className="card section" style={{marginTop:18}}><h2>Foundation capabilities</h2><p>Reusable platform services now available to every future module.</p><div className="quick-grid">{[["Authentication shell","Secure signed session, login and logout"],["RBAC architecture","Permission model and SQL schema ready"],["Audit architecture","Immutable business activity schema"],["Notifications","In-app notification data model"],["Documents","Attachment metadata and controlled storage"],["Deployment","Standalone Next.js and IIS documentation"]].map(([a,b])=><div className="quick-card" key={a}><strong>{a}</strong><span>{b}</span></div>)}</div></section></main></AppShell>}
