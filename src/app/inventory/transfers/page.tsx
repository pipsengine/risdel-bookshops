import {AppShell} from "@/components/app-shell";import {InventoryTabs} from "@/components/inventory-tabs";import {InventoryOperationForm} from "@/components/inventory-operation-form";import {requirePermission} from "@/lib/authz";import {inventoryLookups,listTransfers} from "@/features/inventory/data";import {receiveTransfer} from "@/app/actions/inventory";
export default async function Page(){await requirePermission('inventory.transfers.view');const [lookups,rows]=await Promise.all([inventoryLookups(),listTransfers()]);return <AppShell active="Inventory"><main className="page"><div className="page-header"><div><h1 className="page-title">Stock Transfers</h1><p className="page-desc">Move controlled stock between Risdel Enterprise warehouses and branches.</p></div></div><InventoryTabs active="Transfers"/><div className="catalogue-layout"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Transfer</th><th>Source</th><th>Destination</th><th>Status</th><th>Dispatched</th><th>Created by</th><th></th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.Id}><td><strong>{r.TransferNumber}</strong><span className="table-sub">{r.Notes||'Stock transfer'}</span></td><td>{r.SourceName}</td><td>{r.DestinationName}</td><td><span className={`badge ${r.Status==='RECEIVED'?'badge-success':'badge-warning'}`}>{String(r.Status).replaceAll('_',' ')}</span></td><td>{r.DispatchedAt?new Date(r.DispatchedAt).toLocaleString('en-NG'):'—'}</td><td>{r.CreatedByName||'—'}</td><td>{r.Status==='IN_TRANSIT'&&<form action={receiveTransfer as any}><input type="hidden" name="id" value={r.Id}/><button className="btn btn-xs btn-primary">Receive</button></form>}</td></tr>):<tr><td colSpan={7}>No transfers have been created.</td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Dispatch transfer</h2><p style={{marginBottom:18}}>Stock is removed from the source immediately and added to the destination only when received.</p><InventoryOperationForm mode="transfer" lookups={lookups}/></aside></div></main></AppShell>}
