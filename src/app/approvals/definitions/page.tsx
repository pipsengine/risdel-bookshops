import {AppShell} from "@/components/app-shell";import {ApprovalDefinitionForm} from "@/components/approval-forms";import {approvalWorkspace} from "@/features/approvals/data";import {requirePermission} from "@/lib/authz";
export default async function Page(){await requirePermission("workflow.approvals.configure");const w=await approvalWorkspace();return <AppShell active="Approvals"><main className="page"><div className="page-header"><div><h1 className="page-title">Approval Workflow Rules</h1><p className="page-desc">Configure routing, permissions, thresholds and service-level expectations.</p></div></div><div className="document-detail-grid"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Workflow</th><th>Module</th><th>Trigger</th><th>SLA</th><th>Status</th></tr></thead><tbody>{w.definitions.map(d=><tr key={d.Id}><td><strong>{d.Code}</strong></td><td>{d.Name}</td><td>{d.Module}</td><td>{d.TriggerStatus}</td><td>{d.EscalationHours}h</td><td><span className="badge badge-success">{d.Status}</span></td></tr>)}</tbody></table></div></section><aside className="card section"><h2>New workflow</h2><ApprovalDefinitionForm/></aside></div></main></AppShell>}
