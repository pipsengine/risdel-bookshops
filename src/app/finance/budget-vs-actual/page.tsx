import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {budgetVsActual} from "@/features/finance/management-data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page({searchParams}:{searchParams:Promise<{budget?:string}>}){await requirePermission("finance.budgets.view");const q=await searchParams,x=await budgetVsActual(q.budget);return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Budget vs Actual</h1><p className="page-desc">Compare approved or draft budget values against posted General Ledger actuals.</p></div></div><FinanceTabs active="Budget vs Actual"/><form className="card filter-bar" method="get"><select name="budget" defaultValue={x.budget?.Id||""}>{x.budgets.map((b:any)=><option key={b.Id} value={b.Id}>{b.BudgetCode} — {b.Name}</option>)}</select><button className="btn" type="submit">Apply</button></form><div className="grid-3"><div className="card kpi"><span>Budget</span><strong>{m(x.totalBudget)}</strong></div><div className="card kpi"><span>Actual</span><strong>{m(x.totalActual)}</strong></div><div className="card kpi"><span>Variance</span><strong>{m(x.totalActual-x.totalBudget)}</strong></div></div><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Period</th><th>Account</th><th>Budget</th><th>Actual</th><th>Variance</th><th>%</th></tr></thead><tbody>{x.rows.length?x.rows.map((r:any)=><tr key={r.Id}><td>{r.PeriodCode}</td><td><strong>{r.AccountCode}</strong> — {r.AccountName}</td><td>{m(r.Amount)}</td><td>{m(r.Actual)}</td><td>{m(r.Variance)}</td><td>{Number(r.VariancePercent||0).toFixed(1)}%</td></tr>):<tr><td colSpan={6}>No budget lines available.</td></tr>}</tbody></table></div></section></main></AppShell>}
