import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {ExpenseCategoryForm} from "@/components/expense-forms";import {expenseWorkspace,money} from "@/features/finance/expenses-data";import {requirePermission} from "@/lib/authz";
export default async function Page(){await requirePermission("finance.expenses.configure");const x=await expenseWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Expense Categories</h1><p className="page-desc">Govern expense classifications, ledger mapping, evidence rules and approval thresholds.</p></div></div><FinanceTabs active="Expense Categories"/><div className="document-detail-grid"><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Code</th><th>Category</th><th>GL account</th><th>Receipt required</th><th>Approval threshold</th></tr></thead><tbody>{x.categories.map((c:any)=><tr key={c.Id}><td><strong>{c.Code}</strong></td><td>{c.Name}<small style={{display:"block"}}>{c.Description}</small></td><td>{c.GLAccountCode}</td><td>{String(c.RequiresReceipt)==="true"?"Yes":"No"}</td><td>{money(c.ApprovalThreshold)}</td></tr>)}</tbody></table></div></section><aside className="card section"><h2>Add category</h2><ExpenseCategoryForm/></aside></div></main></AppShell>}
