import {AppShell} from "@/components/app-shell";import {FulfilmentTabs} from "@/components/fulfilment-tabs";import {FulfilmentForm} from "@/components/fulfilment-forms";import {fulfilmentLookups,fulfilmentWorkspace,money} from "@/features/fulfilment/data";import {requirePermission} from "@/lib/authz";import Link from "next/link";
export default async function Page(){await requirePermission("fulfilment.view");const [x,l]=await Promise.all([fulfilmentWorkspace(),fulfilmentLookups()]);const customers=new Map(l.customers.map((c:any)=>[c.Id,c.Name]));const orders=l.orders.map((o:any)=>({...o,CustomerName:customers.get(o.CustomerId)||"Customer"}));return <AppShell active="Fulfilment"><main className="page"><div className="page-header"><div><h1 className="page-title">Delivery & Fulfilment</h1><p className="page-desc">Pack, dispatch, deliver and collect customer orders with end-to-end traceability.</p></div></div><FulfilmentTabs active="Overview"/><div className="grid-4"><div className="card kpi"><span>Pending packing</span><strong>{x.pending}</strong></div><div className="card kpi"><span>In transit</span><strong>{x.inTransit}</strong></div><div className="card kpi"><span>Delivered today</span><strong>{x.deliveredToday}</strong></div><div className="card kpi"><span>Delivery fees</span><strong>{money(x.fees)}</strong></div></div><div className="document-detail-grid" style={{marginTop:16}}><section className="card section"><h2>Active fulfilment jobs</h2><div className="table-wrap"><table className="table"><thead><tr><th>Job</th><th>Order</th><th>Customer</th><th>Type</th><th>Courier</th><th>Status</th></tr></thead><tbody>{x.jobs.slice(0,12).map((j:any)=><tr key={j.Id}><td><Link href="/fulfilment/jobs"><strong>{j.FulfilmentNumber}</strong></Link></td><td>{j.OrderNumber}</td><td>{j.CustomerName}</td><td>{j.FulfilmentType}</td><td>{j.CourierDisplay}</td><td><span className="badge badge-neutral">{j.Status}</span></td></tr>)}</tbody></table></div></section><aside className="card section"><h2>Create fulfilment</h2><FulfilmentForm orders={orders} couriers={l.couriers}/></aside></div></main></AppShell>}
