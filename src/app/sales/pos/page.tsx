import { AppShell } from "@/components/app-shell";import { SalesTabs } from "@/components/sales-tabs";import { PosRegister } from "@/components/pos-register";import { ShiftForm } from "@/components/shift-form";import { requirePermission } from "@/lib/authz";import { posContext } from "@/features/sales/data";
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const actor=await requirePermission("sales.pos.access");const sp=await searchParams;const ctx=await posContext(actor.userId||"",sp.held);return <AppShell active="Sales"><main className="page sales-page"><div className="page-header"><div><h1 className="page-title">Point of Sale</h1><p className="page-desc">Fast barcode/ISBN checkout with stock-aware selling, controlled discounts and multi-method payments.</p></div><span className="badge badge-success">Module 07 · Live</span></div><SalesTabs active="POS"/><div className="sales-kpis"><div className="card mini-kpi"><span>Cashier</span><strong>{actor.name}</strong><small>{ctx.openShift?ctx.openShift.ShiftNumber:"No open shift"}</small></div><div className="card mini-kpi"><span>Sales location</span><strong>{ctx.openShift?ctx.warehouses.find((w:any)=>w.Id===ctx.openShift.WarehouseId)?.Name||"—":"—"}</strong><small>{ctx.openShift?"Ready for transactions":"Open a shift to start"}</small></div><div className="card mini-kpi"><span>Sellable products</span><strong>{ctx.openShift?ctx.inventory.filter((x:any)=>x.WarehouseId===ctx.openShift.WarehouseId).length:0}</strong><small>With available stock</small></div><div className="card mini-kpi"><span>Payment methods</span><strong>{ctx.paymentMethods.length}</strong><small>Configured and active</small></div></div>{ctx.openShift?<PosRegister ctx={ctx}/>:<div className="sales-start-grid"><section className="card section"><h2>POS is locked until a cashier shift is opened</h2><p>A shift binds transactions to a cashier, sales warehouse, branch, and opening cash position.</p><div className="pos-locked"><div><strong>Controlled retail operation</strong><span>Opening and closing shifts allows Risdel Bookshops to reconcile cash, detect variances and attribute every sale.</span></div></div></section><aside className="card section"><h2>Open cashier shift</h2><p style={{marginBottom:18}}>Select the sales warehouse and record the physical opening cash float.</p><ShiftForm warehouses={ctx.warehouses}/></aside></div>}</main></AppShell>}
