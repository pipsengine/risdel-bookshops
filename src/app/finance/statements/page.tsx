import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financialStatements} from "@/features/finance/management-data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page({searchParams}:{searchParams:Promise<{start?:string,end?:string}>}){await requirePermission("finance.statements.view");const q=await searchParams,end=q.end||new Date().toISOString().slice(0,10),start=q.start||`${end.slice(0,4)}-01-01`,d1=new Date(start),d2=new Date(end);d1.setFullYear(d1.getFullYear()-1);d2.setFullYear(d2.getFullYear()-1);const x=await financialStatements(start,end,d1.toISOString().slice(0,10),d2.toISOString().slice(0,10));const assets=x.current.filter((a:any)=>a.AccountType==='ASSET'&&a.Amount),liab=x.current.filter((a:any)=>a.AccountType==='LIABILITY'&&a.Amount),eq=x.current.filter((a:any)=>a.AccountType==='EQUITY'&&a.Amount);return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Financial Statements</h1><p className="page-desc">Management financial statements generated from posted General Ledger entries.</p></div></div><FinanceTabs active="Financial Statements"/><form className="card filter-bar" method="get"><label>From<input name="start" type="date" defaultValue={start}/></label><label>To<input name="end" type="date" defaultValue={end}/></label><button className="btn" type="submit">Run statements</button></form><div className="document-detail-grid"><section className="card section"><h2>Profit & Loss</h2><div className="policy-list"><p><span>Revenue</span><strong>{m(x.revenue)}</strong></p><p><span>Cost of Goods Sold</span><strong>{m(x.cogs)}</strong></p><p><span>Gross Profit</span><strong>{m(x.grossProfit)}</strong></p><p><span>Operating Expenses</span><strong>{m(x.operatingExpenses)}</strong></p><p><span>Net Profit / (Loss)</span><strong>{m(x.netProfit)}</strong></p></div><h3>Prior comparable period</h3><div className="policy-list"><p><span>Revenue</span><strong>{m(x.priorRevenue)}</strong></p><p><span>Gross Profit</span><strong>{m(x.priorGrossProfit)}</strong></p><p><span>Net Profit</span><strong>{m(x.priorNetProfit)}</strong></p></div></section><section className="card section"><h2>Statement of Financial Position</h2><div className="policy-list"><p><span>Total Assets</span><strong>{m(x.assets)}</strong></p><p><span>Total Liabilities</span><strong>{m(x.liabilities)}</strong></p><p><span>Equity before current profit</span><strong>{m(x.equity)}</strong></p><p><span>Current-period Profit</span><strong>{m(x.netProfit)}</strong></p><p><span>Liabilities + Equity + Profit</span><strong>{m(x.liabilities+x.equity+x.netProfit)}</strong></p></div></section></div><section className="card section"><h2>Balance sheet account detail</h2><div className="table-wrap"><table className="table"><thead><tr><th>Account</th><th>Type</th><th>Balance</th></tr></thead><tbody>{[...assets,...liab,...eq].map((a:any)=><tr key={a.Id}><td>{a.AccountCode} — {a.AccountName}</td><td>{a.AccountType}</td><td>{m(a.Amount)}</td></tr>)}</tbody></table></div></section><div className="callout info">These are management statements based on posted entries. Period close, depreciation, accruals, tax adjustments and external-accountant review may still be required before statutory use.</div></main></AppShell>}
