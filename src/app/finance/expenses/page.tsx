import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {ExpenseForm,ExpensePaymentForm} from "@/components/expense-forms";import {approveExpense} from "@/app/actions/expenses";import {expenseWorkspace,money} from "@/features/finance/expenses-data";import {can,requirePermission} from "@/lib/authz";
export default async function Page(){const user=await requirePermission("finance.expenses.view");const mayApprove=can(user,"finance.expenses.approve"),mayPay=can(user,"finance.expenses.pay");const x=await expenseWorkspace();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Expenses</h1><p className="page-desc">Request, approve, pay and account for operating expenses with evidence and audit control.</p></div></div><FinanceTabs active="Expenses"/><div className="grid-4"><div className="card kpi"><span>Pending approval</span><strong>{x.pending}</strong></div><div className="card kpi"><span>Approved unpaid</span><strong>{money(x.unpaid)}</strong></div><div className="card kpi"><span>Paid this month</span><strong>{money(x.paidThisMonth)}</strong></div><div className="card kpi"><span>Petty cash balance</span><strong>{money(x.pettyBalance)}</strong></div></div><div className="document-detail-grid" style={{marginTop:16}}><section className="card section"><h2>Expense register</h2><div className="table-wrap"><table className="table"><thead><tr><th>Expense</th><th>Date</th><th>Category</th><th>Payee</th><th>Total</th><th>Branch</th><th>Status</th><th>Action</th></tr></thead><tbody>{x.expenses.map((r:any)=><tr key={r.Id}><td><strong>{r.ExpenseNumber}</strong><small style={{display:"block"}}>{r.Description}</small></td><td>{r.ExpenseDate}</td><td>{r.CategoryName}</td><td>{r.Payee||"—"}</td><td>{money(r.TotalAmount)}</td><td>{r.BranchName}</td><td><span className="badge badge-neutral">{r.Status}</span></td><td>{["SUBMITTED","PENDING_APPROVAL"].includes(r.Status)&&mayApprove?<div style={{display:"flex",gap:6}}><form action={approveExpense}><input type="hidden" name="id" value={r.Id}/><input type="hidden" name="action" value="APPROVE"/><button className="btn btn-sm">Approve</button></form><form action={approveExpense}><input type="hidden" name="id" value={r.Id}/><input type="hidden" name="action" value="REJECT"/><button className="btn btn-sm">Reject</button></form></div>:r.Status==="APPROVED"&&mayPay?<ExpensePaymentForm expense={r} cash={x.cash} funds={x.funds}/>:"—"}</td></tr>)}</tbody></table></div></section><aside className="card section"><h2>Submit expense</h2><ExpenseForm categories={x.categories} branches={x.branches} cash={x.cash} funds={x.funds}/></aside></div></main></AppShell>}
