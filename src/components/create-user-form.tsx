"use client";
import { useActionState } from "react";
import { createUser, type ActionState } from "@/app/actions/users";
const initial:ActionState={};
export function CreateUserForm({roles}:{roles:any[]}){const [state,action,pending]=useActionState(createUser,initial);return <form action={action} className="form-stack"><div className="form-grid"><div className="field"><label>Full name *</label><input className="input" name="displayName" required placeholder="e.g. Ada Okafor"/></div><div className="field"><label>Email address *</label><input className="input" name="email" type="email" required placeholder="name@risdel.com"/></div><div className="field"><label>Primary role *</label><select className="input" name="roleId" required defaultValue=""><option value="" disabled>Select role</option>{roles.filter(r=>r.IsActive).map(r=><option key={r.Id} value={r.Id}>{r.Name}</option>)}</select></div><div className="field"><label>Temporary password</label><input className="input" name="temporaryPassword" type="text" placeholder="Leave blank to generate securely"/><small>Must meet password policy. User will change it at first sign in.</small></div></div>{state.error&&<div className="alert alert-danger">{state.error}</div>}{state.success&&<div className="alert alert-success"><strong>{state.success}</strong>{state.temporaryPassword&&<div className="temp-password">Temporary password: <code>{state.temporaryPassword}</code></div>}</div>}<div className="form-actions"><button className="btn btn-primary" disabled={pending}>{pending?'Creating…':'Create user'}</button></div></form>}
