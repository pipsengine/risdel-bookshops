import Link from "next/link";import {AppShell} from "@/components/app-shell";import {DocumentsTabs} from "@/components/documents-tabs";import {requirePermission} from "@/lib/authz";import {documentCentre} from "@/features/documents/data";export default async function Page(){await requirePermission("documents.view");const x=await documentCentre(),rows=x.rows.slice(0,50);return <AppShell active="Documents"><main className="page"><div className="page-header"><div><h1 className="page-title">Recent Documents</h1><p className="page-desc">Recently created or updated controlled documents.</p></div></div><DocumentsTabs active="Recent"/><section className="card section"><div className="table-wrap"><table className="table"><thead><tr><th>Document</th><th>Type</th><th>Updated</th></tr></thead><tbody>{rows.map((d:any)=><tr key={d.Id}><td><Link href={`/documents/${d.Id}`}>{d.DocumentNumber} — {d.Title}</Link></td><td>{d.DocumentType}</td><td>{String(d.UpdatedAt||d.CreatedAt).slice(0,16)}</td></tr>)}</tbody></table></div></section></main></AppShell>}