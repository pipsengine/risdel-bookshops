import Link from "next/link";import {AppShell} from "@/components/app-shell";import {DocumentsTabs} from "@/components/documents-tabs";import {requirePermission} from "@/lib/authz";import {documentCentre} from "@/features/documents/data";export default async function Page(){await requirePermission("documents.view");const x=await documentCentre(),rows=x.rows.filter((d:any)=>d.Status==="ARCHIVED");return <AppShell active="Documents"><main className="page"><div className="page-header"><div><h1 className="page-title">Archived Documents</h1><p className="page-desc">Inactive documents retained for history and audit.</p></div></div><DocumentsTabs active="Archived"/><section className="card section"><div className="table-wrap"><table className="table"><tbody>{rows.map((d:any)=><tr key={d.Id}><td><Link href={`/documents/${d.Id}`}>{d.DocumentNumber}</Link></td><td>{d.Title}</td><td>{d.DocumentType}</td><td>{String(d.ArchivedAt||'').slice(0,10)}</td></tr>)}</tbody></table></div></section></main></AppShell>}