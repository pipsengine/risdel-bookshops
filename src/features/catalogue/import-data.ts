import { getRows,SHEETS } from "@/lib/sheets";
const bool=(v:any)=>v===true||String(v).toLowerCase()==="true"||String(v)==="1";
const num=(v:any)=>Number(v||0);
export async function advancedImportDashboard(){const [batches,errors,rollbacks]=await Promise.all([getRows(SHEETS.catalogueImportBatches),getRows(SHEETS.catalogueImportErrors),getRows(SHEETS.catalogueImportRollbacks)]);const sorted=batches.filter((x:any)=>bool(x.IsActive??true)).sort((a:any,b:any)=>String(b.StartedAt||"").localeCompare(String(a.StartedAt||"")));return {batches:sorted.slice(0,50),errors:errors.filter((x:any)=>bool(x.IsActive??true)&&!bool(x.IsResolved)).sort((a:any,b:any)=>String(b.CreatedAt).localeCompare(String(a.CreatedAt))).slice(0,100),rollbacks:rollbacks.filter((x:any)=>bool(x.IsActive??true)).sort((a:any,b:any)=>String(b.CreatedAt).localeCompare(String(a.CreatedAt))).slice(0,20),summary:{batches:sorted.length,completed:sorted.filter((x:any)=>String(x.Status).startsWith("COMPLETED")).length,validated:sorted.filter((x:any)=>String(x.Status).startsWith("VALIDATED")).length,errors:errors.filter((x:any)=>bool(x.IsActive??true)&&!bool(x.IsResolved)).length,products:sorted.reduce((s:number,x:any)=>s+num(x.CreatedProducts),0),stockLines:sorted.reduce((s:number,x:any)=>s+num(x.PostedStockLines),0)}}}
