import {AppShell} from "@/components/app-shell";import {FinanceTabs} from "@/components/finance-tabs";import {requirePermission} from "@/lib/authz";import {financeOverview} from "@/features/finance/data";const m=(v:any)=>new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN"}).format(Number(v||0));export default async function Page(){await requirePermission("finance.cashbank.view");const x=await financeOverview();return <AppShell active="Finance"><main className="page"><div className="page-header"><div><h1 className="page-title">Cash & Bank</h1><p className="page-desc">Controlled cash/bank account register and settlement visibility.</p></div></div><FinanceTabs active="Cash & Bank"/><div className="grid-4">{x.cash.map((a:any)=><div className="card kpi" key={a.Id}><span>{a.Name}</span><strong>{m(a.CurrentBalance)}</strong><small>{a.AccountType} · {a.CurrencyCode}</small></div>)}</div><section className="card section" style={{marginTop:16}}><h2>Cashier settlements awaiting financial posting</h2><p>{x.settlements.length} settlement record(s) available from Cash Management.</p></section></main></AppShell>}
