"use server";
import { revalidatePath } from "next/cache";
import sql from "mssql";
import { requirePermission } from "@/lib/authz";
import { getDb } from "@/lib/db";
import { generateTemporaryPassword, hashPassword, validatePasswordPolicy } from "@/lib/password";
import { writeAudit } from "@/lib/audit";
export type ActionState={error?:string;success?:string;temporaryPassword?:string};
export async function createUser(_:ActionState,formData:FormData):Promise<ActionState>{const actor=await requirePermission("admin.users.manage");const name=String(formData.get("displayName")||"").trim();const email=String(formData.get("email")||"").trim().toLowerCase();const roleId=String(formData.get("roleId")||"");let password=String(formData.get("temporaryPassword")||"");if(!name||!email||!roleId)return {error:"Name, email and role are required."};if(!password)password=generateTemporaryPassword();const policy=validatePasswordPolicy(password);if(policy.length)return {error:`Temporary password must contain ${policy.join(", ")}.`};try{const db=await getDb();const exists=await db.request().input("email",sql.NVarChar(256),email).query(`SELECT 1 x FROM auth.Users WHERE LOWER(Email)=@email`);if(exists.recordset.length)return {error:"A user with this email already exists."};const hash=await hashPassword(password);const result=await db.request().input("email",sql.NVarChar(256),email).input("name",sql.NVarChar(150),name).input("hash",sql.NVarChar(500),hash).input("roleId",sql.UniqueIdentifier,roleId).input("actor",sql.UniqueIdentifier,actor.userId).query(`DECLARE @id uniqueidentifier=NEWID();INSERT auth.Users(Id,Email,DisplayName,PasswordHash,MustChangePassword,IsActive) VALUES(@id,@email,@name,@hash,1,1);INSERT auth.UserRoles(UserId,RoleId,AssignedBy) VALUES(@id,@roleId,@actor);SELECT @id Id`);const id=String(result.recordset[0].Id);await writeAudit({userId:actor.userId,action:"CREATE",module:"administration",entityType:"User",entityId:id,description:`Created user ${email}`,newValues:{name,email,roleId}});revalidatePath("/administration/users");return {success:"User created successfully. Share the temporary password securely; it will not be shown again.",temporaryPassword:password}}catch(error){return {error:error instanceof Error?error.message:"Unable to create user."}}
}
export async function toggleUser(formData:FormData){const actor=await requirePermission("admin.users.manage");const id=String(formData.get("id")||"");const active=String(formData.get("active"))==="true";if(!id||id===actor.userId)return;const db=await getDb();await db.request().input("id",sql.UniqueIdentifier,id).input("active",sql.Bit,active).query(`UPDATE auth.Users SET IsActive=@active,UpdatedAt=SYSUTCDATETIME() WHERE Id=@id;IF @active=0 DELETE auth.UserSessions WHERE UserId=@id`);await writeAudit({userId:actor.userId,action:active?"ACTIVATE":"DEACTIVATE",module:"administration",entityType:"User",entityId:id,description:`${active?"Activated":"Deactivated"} user account`});revalidatePath("/administration/users")}
export async function unlockUser(formData:FormData){const actor=await requirePermission("admin.users.manage");const id=String(formData.get("id")||"");const db=await getDb();await db.request().input("id",sql.UniqueIdentifier,id).query(`UPDATE auth.Users SET FailedLoginCount=0,LockedUntil=NULL,UpdatedAt=SYSUTCDATETIME() WHERE Id=@id`);await writeAudit({userId:actor.userId,action:"UNLOCK",module:"security",entityType:"User",entityId:id,description:"Unlocked user account"});revalidatePath("/administration/users")}
export async function resetUserPassword(_:ActionState,formData:FormData):Promise<ActionState>{const actor=await requirePermission("admin.users.manage");const id=String(formData.get("userId")||"");let password=String(formData.get("temporaryPassword")||"");if(!password)password=generateTemporaryPassword();const policy=validatePasswordPolicy(password);if(policy.length)return {error:`Temporary password must contain ${policy.join(", ")}.`};const db=await getDb();const hash=await hashPassword(password);await db.request().input("id",sql.UniqueIdentifier,id).input("hash",sql.NVarChar(500),hash).query(`INSERT auth.PasswordHistory(UserId,PasswordHash) SELECT Id,PasswordHash FROM auth.Users WHERE Id=@id AND PasswordHash IS NOT NULL;UPDATE auth.Users SET PasswordHash=@hash,MustChangePassword=1,FailedLoginCount=0,LockedUntil=NULL,UpdatedAt=SYSUTCDATETIME() WHERE Id=@id;DELETE auth.UserSessions WHERE UserId=@id`);await writeAudit({userId:actor.userId,action:"PASSWORD_RESET",module:"security",entityType:"User",entityId:id,description:"Administrator reset user password"});return {success:"Password reset. The user must change it on next sign in.",temporaryPassword:password}}
export async function updateUser(_:ActionState,formData:FormData):Promise<ActionState>{const actor=await requirePermission("admin.users.manage");const id=String(formData.get("userId")||"");const name=String(formData.get("displayName")||"").trim();const phone=String(formData.get("phone")||"").trim();const jobTitle=String(formData.get("jobTitle")||"").trim();const roleId=String(formData.get("roleId")||"");if(!id||!name||!roleId)return {error:"Name and role are required."};const db=await getDb();const tx=new sql.Transaction(db);await tx.begin();try{await new sql.Request(tx).input("id",sql.UniqueIdentifier,id).input("name",sql.NVarChar(150),name).input("phone",sql.NVarChar(50),phone||null).input("job",sql.NVarChar(120),jobTitle||null).input("actor",sql.UniqueIdentifier,actor.userId).query(`UPDATE auth.Users SET DisplayName=@name,Phone=@phone,JobTitle=@job,UpdatedAt=SYSUTCDATETIME(),UpdatedBy=@actor WHERE Id=@id`);await new sql.Request(tx).input("id",sql.UniqueIdentifier,id).query(`DELETE auth.UserRoles WHERE UserId=@id`);await new sql.Request(tx).input("id",sql.UniqueIdentifier,id).input("role",sql.UniqueIdentifier,roleId).input("actor",sql.UniqueIdentifier,actor.userId).query(`INSERT auth.UserRoles(UserId,RoleId,AssignedBy) VALUES(@id,@role,@actor)`);await tx.commit();await writeAudit({userId:actor.userId,action:"UPDATE",module:"administration",entityType:"User",entityId:id,description:"Updated user profile and role",newValues:{name,phone,jobTitle,roleId}});revalidatePath(`/administration/users/${id}`);revalidatePath("/administration/users");return {success:"User details updated."}}catch(error){await tx.rollback();return {error:error instanceof Error?error.message:"Unable to update user."}}
}
