import {AppShell} from '@/components/app-shell';import {AdminTabs} from '@/components/admin-tabs';import {requirePermission} from '@/lib/authz';import {healthData} from '@/features/administration/data';import {recordBackup} from '@/app/actions/administration';
export default async function Page(){await requirePermission('admin.health.view');const x=await healthData();return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">System health & backup</h1><p className="page-desc">Persistence diagnostics, configuration footprint and controlled backup/export requests.</p></div><span className={`badge ${x.health.status==='Healthy'?'badge-success':'badge-warning'}`}>{x.health.status}</span></div><AdminTabs active="Health & Backup"/><div className="grid grid-4" style={{marginTop:18}}><div className="card kpi"><span className="kpi-label">Companies</span><strong className="kpi-value">{x.companies.length}</strong></div><div className="card kpi"><span className="kpi-label">Branches</span><strong className="kpi-value">{x.branches.length}</strong></div><div className="card kpi"><span className="kpi-label">Warehouses</span><strong className="kpi-value">{x.warehouses.length}</strong></div><div className="card kpi"><span className="kpi-label">Backup runs</span><strong className="kpi-value">{x.backups.length}</strong></div></div><section className="card section"><h2>Backup / export control</h2><p>Registers a controlled backup/export request. The existing <code>npm run sheets:export</code> utility remains available for server-side execution.</p><form action={recordBackup} className="form-grid"><label>Backup type<select name="type"><option value="GOOGLE_SHEETS_EXPORT">Google Sheets JSON Export</option><option value="FULL_DATA_EXPORT">Full Data Export</option></select></label><label>Notes<input name="notes" placeholder="Reason or retention reference"/></label><div><button className="btn btn-primary">Request backup</button></div></form></section><section className="card section"><h2>Recent backup/export runs</h2><div className="table-wrap"><table className="table"><thead><tr><th>Number</th><th>Type</th><th>Status</th><th>Started</th><th>Notes</th></tr></thead><tbody>{x.backups.map((b:any)=><tr key={b.Id}><td>{b.BackupNumber}</td><td>{b.BackupType}</td><td>{b.Status}</td><td>{String(b.StartedAt).slice(0,16)}</td><td>{b.Notes||'—'}</td></tr>)}</tbody></table></div></section></main></AppShell>}
