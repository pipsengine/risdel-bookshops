import { AppShell } from "@/components/app-shell";import { CatalogueTabs } from "@/components/catalogue-tabs";import { MasterDataForm } from "@/components/master-data-form";import { requirePermission } from "@/lib/authz";import { listMaster } from "@/features/catalogue/data";
export default async function Page(){await requirePermission('catalogue.master.view');const rows=await listMaster('catalogueAuthors');return <AppShell active="Catalogue"><main className="page"><div className="page-header"><div><h1 className="page-title">Authors</h1><p className="page-desc">Controlled catalogue master data used across Risdel Bookshops.</p></div><span className="badge badge-info">{rows.filter((x:any)=>x.IsActive).length} active</span></div><CatalogueTabs active="Authors"/><div className="catalogue-layout master-layout"><section className="card section"><div className="section-heading"><div><h2>Authors directory</h2><p>Reusable records prevent duplicate free-text values in product setup.</p></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Code</th><th>Description</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map((x:any)=><tr key={x.Id}><td><strong>{x.DisplayName||x.Name}</strong></td><td>{x.Code||'—'}</td><td className="muted">{x.Description||'—'}</td><td><span className={`badge ${x.IsActive?'badge-success':'badge-neutral'}`}>{x.IsActive?'Active':'Inactive'}</span></td></tr>):<tr><td colSpan={4}><div className="catalogue-empty"><strong>No authors yet.</strong><span>Add the first record using the form on the right.</span></div></td></tr>}</tbody></table></div></section><aside className="card section sticky-panel"><h2>Add author</h2><p style={{marginBottom:18}}>Create a reusable author record for product setup and reporting.</p><MasterDataForm kind="author" label="Author"/></aside></div></main></AppShell>}
