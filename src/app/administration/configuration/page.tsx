import {AppShell} from '@/components/app-shell';import {AdminTabs} from '@/components/admin-tabs';import {requirePermission} from '@/lib/authz';import {configurationData} from '@/features/administration/data';import {saveSetting,toggleFeature} from '@/app/actions/administration';
export default async function Page(){await requirePermission('admin.configuration.view');const x=await configurationData();return <AppShell active="Administration"><main className="page"><div className="page-header"><div><h1 className="page-title">System configuration</h1><p className="page-desc">Central operating parameters, feature controls, payment methods, periods and integrations.</p></div></div><AdminTabs active="Configuration"/><div className="grid grid-4" style={{marginTop:18}}><div className="card kpi"><span className="kpi-label">Settings</span><strong className="kpi-value">{x.settings.length}</strong></div><div className="card kpi"><span className="kpi-label">Sequences</span><strong className="kpi-value">{x.sequences.length}</strong></div><div className="card kpi"><span className="kpi-label">Feature flags</span><strong className="kpi-value">{x.features.length}</strong></div><div className="card kpi"><span className="kpi-label">Integrations</span><strong className="kpi-value">{x.integrations.length}</strong></div></div><section className="card section"><h2>System settings</h2><div className="table-wrap"><table className="table"><thead><tr><th>Category</th><th>Setting</th><th>Value</th><th>Type</th><th></th></tr></thead><tbody>{x.settings.map((s:any)=><tr key={s.Id}><td>{s.Category}</td><td><strong>{s.SettingKey}</strong><div className="muted">{s.Description}</div></td><td><form action={saveSetting} style={{display:'flex',gap:8}}><input type="hidden" name="id" value={s.Id}/><input name="value" defaultValue={s.SettingValue??''}/><button className="btn btn-secondary">Save</button></form></td><td>{s.DataType}</td><td>{String(s.IsEncrypted).toLowerCase()==='true'?'Protected':''}</td></tr>)}</tbody></table></div></section><section className="card section"><h2>Feature controls</h2><div className="table-wrap"><table className="table"><thead><tr><th>Feature</th><th>Module</th><th>Status</th><th></th></tr></thead><tbody>{x.features.map((f:any)=><tr key={f.Id}><td><strong>{f.Name}</strong><div className="muted">{f.Description}</div></td><td>{f.Module}</td><td><span className={`badge ${String(f.IsEnabled).toLowerCase()==='true'?'badge-success':'badge-warning'}`}>{String(f.IsEnabled).toLowerCase()==='true'?'Enabled':'Disabled'}</span></td><td><form action={toggleFeature}><input type="hidden" name="id" value={f.Id}/><input type="hidden" name="enabled" value={String(f.IsEnabled).toLowerCase()==='true'?'true':'false'}/><button className="btn btn-secondary">Toggle</button></form></td></tr>)}</tbody></table></div></section><div className="grid grid-2"><section className="card section"><h2>Payment methods</h2>{x.payments.map((p:any)=><p key={p.Id}><strong>{p.Code}</strong> — {p.Name} <span className="muted">{p.MethodType}</span></p>)}</section><section className="card section"><h2>Financial periods</h2>{x.periods.map((p:any)=><p key={p.Id}><strong>{p.PeriodCode}</strong> — {p.Name} <span className="badge">{p.Status}</span></p>)}</section></div><section className="card section"><h2>Integration registry</h2><div className="table-wrap"><table className="table"><thead><tr><th>Integration</th><th>Type</th><th>Provider</th><th>Status</th></tr></thead><tbody>{x.integrations.map((i:any)=><tr key={i.Id}><td>{i.Name}</td><td>{i.IntegrationType}</td><td>{i.Provider}</td><td>{i.Status}</td></tr>)}</tbody></table></div></section></main></AppShell>}
